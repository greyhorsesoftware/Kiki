# 31 — Final implementation plan for 0.1.0

**Status:** plan, written 2026-09-19. Nothing here is built yet. Supersedes the "Order" section of `29-release-readiness.md`; where this document and an older plan disagree, this one wins and the older plan is amended in phase 9.

Builds on: every plan. Inputs: `29-release-readiness.md`, `30-code-health.md`, `activity-view-spec.md`, and a read-only audit of every plan's Verification list against the tree (2026-09-19; five passes, nothing built or run). Findings the audit reached by reading code and did not run are marked **(probable)** — reproduce before fixing.

## What the audit changed

Plan 29 opens with "every numbered plan has code behind it". That is not the whole truth:

- **Plan 13's code viewer was deleted** (`CodeTab.qml`, commit 800883b); only the daemon half and the nvim bridge remain. **Plan 16 (project mode) is a prototype** with four probable defects, one of which may leave no keyboard way out of the mode. **Plan 12 (search)** works but none of its Verification bullets has been measured, and its scope UI is wired only into the portal dialog.
- **Plan 29 is stale in two places.** Section H: remote copy/move/delete/rename now exist (`kikid/src/transfer.rs`) and `remote_transfers.py` proves all nine end-pairs against a real `sshd` and a pyftpdlib FTPS server — which also settles section A's `vsftpd` question (pyftpdlib replaces it) and provides the `sshd` fixture. Section K: Columns and Gallery are already drop targets; List, Icon and Columns are drag sources.
- **Defects nobody had listed**: extract-undo can delete files that were there before; job sessions are never requested, so a transfer shares the browser's connection; a cancelled trash/delete ends as `done`; a second launch probably opens a second window; the package's install script sets root's folder handler; the share path leaks its temp directory on every error; a `git commit` in a terminal leaves badges stale.

## Decisions taken (owner, 2026-09-19)

1. **Location kinds**: `sftp`, `ftps`, and **gio** (`smb`, `dav`). `mtp`, `afc`, `ptp` are not in 0.1.0.
2. **Drag and drop ships** (plan 29 K) — between panes, modifiers, local/remote defaults. **Spring-loaded folders do not**; a later release.
3. **The activity popup carries the full entry anatomy** of `activity-view-spec.md` (it is how downloads and uploads are watched), placed as plan 29 I says: an orb at the bottom right, the popup anchored above it.
4. **The in-app Jarvis panel is removed.** "Open AI here…" and "Open Terminal here…" stay.
5. **Pointer testing**: `sway` headless was chosen for section C. The audit then found that `wlrctl` cannot hold a button down, so no headless compositor gives a real drag with the tools we have. Sway is kept as a time-boxed spike for *clicks*; drags are proven through an IPC drop hook (phase 5) and by hand.
6. **Everything in plan 29 F is fixed**, the video player included (reproduce the no-sound report first: the code has sound on, and the machine's default sink was HDMI).

## Defaults chosen here — each one is the owner's to overrule

These were "build it or strike it from the plan" choices the audit turned up. The rule applied: **fix what is wrong on screen or loses data; strike or defer what is merely promised.** Overruling one moves it from phase 9 (a doc edit) into the phase named.

| # | Item | Default | If overruled |
|---|---|---|---|
| D1 | Plan 13 code viewer (Code tab) | **decided (owner, 2026-09-19): kiki has no built-in code viewer — not deferred, not wanted.** Code is read in an editor, which is what Open in and the nvim bridge are for. The daemon half that outlived `CodeTab.qml` is removed in phase 1; plan 13 shrinks to the editor bridge | — |
| D2 | Plan 16 project mode | **fix the four defects, verify by hand, ship as it is**; everything else in the plan is post-0.1.0. If the hand pass is bad, hide the entry point instead | 3–3.5 days for the full plan |
| D3 | Plan 14 Open-in: Settings page, TOML watcher, `tab` placement, `follow`/`on_save`, `local_uri` fallback, malformed-entry report | post-0.1.0 | 1–1.5 days |
| D4 | Plan 20 Settings pages: Locations, Open in, Plugins (Devices is moot) | post-0.1.0 | 1–2 days |
| D5 | Plan 12 search as written (sorted index, streaming remote walk, scope menu, cancel, `IndexProgress`) | trim the plan to what is built; fix the two bugs; add tests | 5–7 days |
| D6 | Plan 27 gallery: Ctrl+wheel zoom, video poster, remote large preview, Filmstrip setting | post-0.1.0. **Fixed regardless**: `Del` advances, the image count, type-ahead off in gallery. **Decided (owner, 2026-09-19): a folder with no pictures is not an empty state** — Gallery shows its files and folders with the icons Icon view uses, scaled up (phase 2) | ~1.5 days |
| D7 | Plan 03 inspector: Created field, editable name, Git branch + last commit | Git detail **built** (the `GitStatus` request exists; ~2 h). **Decided (owner, 2026-09-19): the inspector's name is not editable** — renaming is done in the views (`F2`), and a second place to do it is a second thing to keep right; struck from plan 03, not deferred. Created field post-0.1.0 | Created: ~3 h (`statx` btime into `Meta`) |
| D8 | Plan 05 archives: "Extract here" folder rule, "Extract to…" chooser | **built** — extracting thirty files loose into the current folder is a wrong result, not a missing feature (half a day); `tar.bz2` added to the dialog | — |
| D9 | Plan 01 `Prefetch` (daemon has it, shell never sends it); mallopt A/B | strike Prefetch from the plan; record the mallopt numbers as they are and leave the calls in | half a day |
| D10 | `API-DAEMON.md` events never emitted: `Gone`, `FavoritesChanged` (the shell listens for both), `IndexProgress`, `SharePluginsChanged` | **emit the first two** (a second window's favourites go stale without it); delete the other two from the doc | — |
| D11 | `Hello` version check; `Read`/`Write` streaming text in the API doc | build the version check (1 h); delete the streaming text | — |
| D12 | D-Bus activation of `org.kiki.App` | drop `DBusActivatable=true` and the `.service` file | half a day to implement |
| D13 | Portal chooser over a remote location returns an `sftp://` URI | hide Locations in the chooser | half a day to fetch to a temp file |
| D14 | gio: `smb://` scheme handler + dialog prefill; "Encryption Required" (a field that does nothing); AFP | handler post-0.1.0; **remove the field**; AFP not shipped | half a day; 1–3 h; minutes |
| D15 | gio automated test against `smbd` | hand-verified only for 0.1.0, said so in its Status line | 1 day |
| D16 | Plan 08 "Edit rules…" filter editor | post-0.1.0 | half a day |
| D17 | Activity popup "Log" button | **decided (owner, 2026-09-19): built.** The audit said "the daemon has no per-job log", which is true and beside the point: the libraries underneath already write one and nobody is listening. `russh`, `russh-sftp`, `suppaftp` and `rustls` all log through the `log` facade, and no plugin installs a logger, so every line is dropped; `glib` has its own hook (`log_set_writer_func`). Capture it per job — phase 3 | — |
| D18 | Plan 24: a picture folder with no memory opens in **Gallery** (code) vs Icon (doc) | the code is right; amend the doc, add a test | — |
| D19 | Plan 23: `Backspace` is "parent folder" (code) vs "back" (doc and cheat sheet) | the code is right; fix doc and cheat sheet | — |
| D20 | Plan 11 visual-diff layer and shell performance probes; plan 26 shell half | post-0.1.0 — a suite that cannot be built in time must not hold the tag; the theme pass stays on the manual list | 3+ days |
| D21 | Remote copy is not undoable (`transfer.rs`, by decision there) vs plan 29 K's "undoes behind a confirmation" | keep the code; amend K | 1 day |
| D22 | Copy small files with parallel workers (plan 04) | post-0.1.0; phase 4's throughput numbers say whether it matters | — |
| D23 | `30-code-health.md` W6–W8 (Label/Rule, Shell's repeated handlers, Rust repeats) | 0.1.1 — wide, mechanical, and in conflict with everything in this plan | 1–1.5 days |

## Order

Eleven phases. Each ends with `make test` green and a commit (or several, by topic). Sizes are working days for one person and are the audit's estimates, not promises.

### Phase 0 — a clean tree (½ day) — **done 2026-09-19**

- The tree was committed whole (`7541e5d`), not split by topic; one local commit, left as it is.
- `Makefile` (30 W5): `test-qml` runs offscreen (`QT_QPA_PLATFORM ?= offscreen`, the environment wins); `clippy` is a target of its own and the first thing `make test` runs; `lint` is `clippy` plus the format check. `jobs::wait`'s poll stays in phase 2.
- Clippy at zero: the seven warnings fixed (two by hand — a `Chunk` type alias in the SFTP plugin, a doc list in LocalSend), and the empty `impl Inner {}` and `impl Listing {}` deleted.
- **The audit was wrong about FTPS**: `make e2e-servers` exists, `tests/e2e/.venv` is there, and the FTPS pairs of `remote_transfers` run on this machine. Nothing to do.
- **Baseline, 2026-09-19, clippy clean**: `cargo test` 145 passed, 0 failed; `make test-qml` (offscreen) 300 passed, 0 failed; `make test-e2e` under `cage` 183 passed, 0 failed, 1 skipped (`pointer_ops`, the virtual pointer). These replace plan 29's 112 / 155 / 85.

### Phase 1 — make the build match the decisions (1½ days) — **done 2026-09-19**

**As built.** Suites after it: clippy clean; `cargo test` 144 (two panel tests out, one in); `make test-qml` 306; `make test-e2e` 183 passed, 1 skipped; no error in the shell's log.

- **Jarvis panel**: gone as listed below — `ai.rs` is 203 lines of 408. One thing went further than planned: **a custom command is now run as written**, with `{prompt}` standing for the opening message and the word holding it left out when nothing is selected (`ai::custom_argv`, tested) — before, only its first word was run. Settings → Jarvis is **Settings → AI**, and its four texts describe a terminal, not a panel. `19-ai-query.md` is already the stub phase 9 asked for; `API-DAEMON.md` has `AiOpen` and `OpenTerminal` and has lost `AiQuery` and `AiCancel`.
- **Code viewer**: `kiki-plugin-highlight` is deleted with its sixteen grammars (249 lines out of `Cargo.lock`), and `OpenText`, `TextFind`, `text_window`, the per-client `texts` map, `helpers::highlight` and `editor.tabWidth` with it. `API-DAEMON.md` follows.
- **Devices**: `devices::openable` drops any device whose kind this build does not ship, and `devices::start` does not spawn its thread when none does; the sidebar already hid an empty section. Tested, with an assertion that reminds whoever ships a device kind to look here.
- **gio**: `LOCATION_KINDS` is `dav`, `ftps`, `sftp`, `smb`; `make build` makes the `kiki-plugin-smb` and `-dav` symlinks the package makes; the Encryption field is gone; both path fields are on a Locations page. Asked of a daemon built from the tree: SMB available; **WebDAV not — "install gvfs-dnssd"**, which is the unavailable path working: `LocationDialog` shows the plugin's reason in place of the form and will not add (`tst_LocationUnavailable.qml`). **Correction to the text below: on Arch the WebDAV backend is in `gvfs-dnssd`; there is no `gvfs-dav`.** The PKGBUILD's optdepends are `gvfs-smb`, `gvfs-dnssd`, `gvfs-wsdd`. Phase 8 needs `sudo pacman -S gvfs-dnssd` first.
- **CI**: differs from the text below on purpose. The device plugins stay in the tree, so one CI job still builds them (`-p kiki-plugin-mtp -p …-ptp -p …-afc`) and is the only one that installs their libraries — otherwise they rot unseen. The release workflow and the AUR `kiki-bin` package no longer name them; `kiki-bin`'s depends now match the main PKGBUILD. There is no macOS CI job, so nothing to exclude.

**As planned:**

**Jarvis panel out** (½ day). Delete `AiPanel.qml`; in `Shell.qml` the `aiOpen` / `aiQuery()` / `aiStatus` / `loadAi` members, IPC `aiQuery` and `aiClose`, the panel instance and its term in the width expression; in `server.rs` the `AiQuery` and `AiCancel` arms; in `ai.rs` `attachment`, `local_answer`, `query`, `run_cli`, `cli_children`, `cancel`, `cli_for` and their two tests (~230 of 408 lines), rebasing `status()` on `interactive_for`. Keep everything "Open AI here…" uses (`PROVIDERS`, `provider`, `interactive_for`, `opening_prompt`, `terminal_argv`, `open_external`, `AiStatus`, `AiConfigure`, `AiOpen`, `OpenTerminal`). **Settings → Jarvis**: keep status, provider and custom command; rewrite the four texts that describe a panel and print mode — and either honour `{prompt}`/`{files}` in a custom command or say that only the command itself is run. "ask Jarvis" → "open AI here" in `ShortcutsOverlay.qml:57` and `config.rs:204`; PKGBUILD's poppler line loses "and text for Jarvis". Design: `Jarvis.dc.html`, its `gen.py` block, `canvas.json`.

**Code viewer's remains out** (2–3 h; D1). `CodeTab.qml` went in 800883b; what it talked to is still built, shipped and called by nothing. Remove `plugins/kiki-plugin-highlight` (the directory, its `members` and `default-members` entries, `highlight` in the PKGBUILD's install loop) — with it go the sixteen tree-sitter grammars, their C builds and a slice of compile time and package size; `helpers::highlight()`; the `OpenText` and `TextFind` arms in `server.rs` and `open_text` behind them (`server.rs:187-200`, `841-865`), plus the `access::record` call made from `OpenText`; `editor.tabWidth` in `config.rs:119` and `Settings.qml:19` (never read). Check first that nothing else asks the highlight helper for anything — on 2026-09-19 the only callers were those two arms. The inspector's text and Markdown preview (`preview.rs`) is not part of this and stays. `API-DAEMON.md` loses the two requests; `API-PLUGIN.md`'s "service request sets are documented in plans 09, 13 and 19" becomes "plan 09".

**Devices off** (2 h). `devices::start()` runs in every build and the sidebar draws Devices, so a plugged-in phone shows and fails to open with `Unsupported`. Gate detection and the sidebar section on `plugin::ships`. Test: no Devices section when no device kind ships.

**gio in** (1 day).
- `LOCATION_KINDS` += `"smb"`, `"dav"` (both arms; never `"gio"` — kinds are the binary-name suffix). `default-members` += `plugins/kiki-plugin-gio`. PKGBUILD: install as `kiki-plugin-smb` with a `kiki-plugin-dav` symlink; `depends` += `glib2`, `gvfs`; `optdepends` `gvfs-smb`, `gvfs-dav` (**check** that Arch splits DAV out, and whether discovery wants `gvfs-wsdd`); `makedepends` += `glib2`.
- Dev and e2e runs point `KIKI_PLUGIN_DIR` at `target/release`, where only `kiki-plugin-gio` exists: the Makefile and `run.sh` create the two symlinks, or SMB and DAV are invisible to every test.
- `LocationDialog` honours `available` / `unavailableReason` (the plugin and SDK send them; nothing in `qml/` reads them) — the reason instead of the form, with a QML test.
- Remove the Encryption field (D14). Move the forms onto `on_page` / `field_in` so they match SFTP and FTPS (Connection | Locations; port on Host's line).
- CI and release workflows, and `packaging/aur/kiki-bin/PKGBUILD`: drop `libmtp`, `libgphoto2`, `libimobiledevice`, `usbmuxd` and the `-p kiki-plugin-{mtp,ptp,afc}` builds; add what the main PKGBUILD has (`libsecret`, `libarchive`, `udisks2`, `git`); macOS CI excludes the gio plugin.

### Phase 2 — defects (5 days) — **in progress**

**Done 2026-09-19: a job's own connection, and uploads that cannot be half files.** Suites after it: clippy clean; `cargo test` 144; QML 306; e2e 203 passed (20 new), 1 skipped.

- **Job sessions went deeper than the audit said.** It was not only that the daemon always asked for the `browse` role: the plugin protocol carried no role on a request at all, and both plugins looked every operation up under `browse` — so the `job` sessions they could open were never used by anything. And the FTPS plugin keeps a session behind one mutex which a transfer holds for its whole length, so **browsing an FTPS location froze for as long as any upload to it ran**. Now: every request names its session (`Session::req` is the only place a plugin request is started, so the role cannot be forgotten); the SDK hands it to the plugin as `sdk::current_role()`; SFTP and FTPS look their session up by it. A job that transfers, deletes, shares or mirrors enters `locations::JobSessions`, which gives it sessions of its own (`job-<id>`), opened only when it first touches a server and closed when it ends, however it ends. `mkdir` and `rename` are one round trip and stay on the browser's. A job's SFTP session is probed for fast scan like the browser's — a mirror scans too.
- **A cancelled upload was reported as done, and left a short file under the real name** — found by the new test, on SFTP and FTPS alike. A plugin is told a write is over by the bytes stopping and cannot tell "that was all" from "we stopped"; the daemon stopped sending on cancel **and on a local read error**, so either produced a truncated file and a successful job. Mirror uploads share the code. Uploads now go to `<name>.kiki-part` and take their name only when every byte has arrived (`mirror::execute::put`, used by all three upload pairings); anything else deletes the part and fails. SFTP's rename refuses an existing target, so one in the way is deleted first.
- **Downloads had the mirror image**: a failed local write (a full disk) was ignored and the short file renamed into place. Now an error. And the download's part name *replaced* the extension, so `notes.txt` and `notes.md` arriving on two mirror workers shared `notes.kiki-part`; it is appended now.
- **Tests**: `remote_transfers` — for each real server: when the jobs are over none of their connections is left; a 768 MB upload runs on a connection of its own (counted at the server's port); a folder never listed before lists while it runs, and the upload is confirmed still running at that moment; cancelled mid-file it ends `cancelled`, leaving neither a file nor a part file; the panes' connection survives; the job's closes. `plugin_contract` — a changed file replaces the replica's with no part left; an upload cancelled before its first byte leaves the replica's file exactly as it was.
- **Known and left**: a plugin process carries one binary stream at a time (`Plugin::binary`, the SDK's `stream_lock`), so while a transfer to a location runs, *previews and thumbnails* from that same kind of location wait; listings do not. Lifting it means a second plugin process for jobs — after 0.1.0 unless phase 4 shows it hurts.
- **Owed to phase 6**: `mirror_sftp` must upload a *changed* file, to prove the delete-then-rename fallback against a real `sshd` (the stub's rename overwrites).

**Done 2026-09-19, second pass: archives, half-finished jobs, share's fetch, search.** Suites after it: clippy clean; `cargo test` 150; QML 307; e2e 210 passed, 1 skipped.

- **Extracting never merges, so undo is exact** (D8 with it). `archive::extract` unpacks in a staging folder inside the destination and then takes ONE free name there: the archive's single item as itself, or — several items — a folder named after the archive (`site.tar.gz` → `site`, then `site (2)`). Nothing already there is merged into or overwritten, and the inverse is "delete that one thing". Before, an archive holding `src/` extracted beside an existing `src/` merged into it, and undo deleted the folder, the user's files with it. "Extract to…" now asks where (kiki's own folder chooser, through a new `Ops.folderNeeded`) instead of silently making a folder; the same rule decides what lands. The compress dialog offers `tar.bz2`.
- **The traversal guard is real and tested.** It read the member list through `list`, which stops at 10,000 entries — so entry 10,001 was never checked. It reads every name now (`bsdtar -tf`); the refusal is a typed `VfsError::Unsafe` (code `Unsafe`), and nothing is created, not even the destination. Tested with a real `..` archive, in Rust and end to end.
- **A job that stops half way can be undone.** `Job::undo_so_far` records the inverse as the work goes; the runner journals it when the job fails or is cancelled. Trash and local copy/move use it (a copy records its target *before* it is whole, so half a folder can be taken back; an undo's delete forgives a thing that was never made). And a job that left its loop early on cancel — trash, delete — now ends `cancelled`, not `done`. This is the local half of phase 4's "a cancelled big copy journals nothing".
- **Share fetches through the transfer code** instead of a copy of it: a remote folder arrives as a folder (it was `Read` as a file), with the job's progress and cancel, a full disk is an error, each item in a folder of its own, and the temp directory goes on every way out (a `Drop` guard). *Owed to phase 7:* a test of it, which needs the stub share plugin.
- **Search.** The index ranked only the first 40,000 matches it met, so the best hit — the exact name — could be one it never reached; it ranks every match now, holding the best 10,000 in a heap. `capped` is the index's own answer (exactly 10,000 was reported as "more"). A location whose plugin has no recursive scan — FTPS; SFTP without a shell — is walked folder by folder instead of finding nothing, and is capped like the index. Tested against both real servers. *Known and left (D5):* a location search still runs in the client's request thread, so that window's other requests wait for it.

**Done 2026-09-19, third pass: git status (plan 15).** Suites after it: clippy clean; `cargo test` 150; QML 316; e2e 217 passed, 1 skipped.

- **Column view** draws the list's letter, in the list's colours, in every column, and dims ignored rows. A column opened from another now takes the pane's daemon rather than the global one — no difference in the app, and what lets a test see an opened column at all.
- **One answer for every view**: `Format.gitMark(row)` and `Format.gitDimmed(row)` are what List, Icon and Columns all ask, so they cannot disagree, and they are where the settings act. **`[git] folders = "off"`** takes the mark off folders; **`showIgnored = "normal"`** stops the dimming (icon tiles dim too now; they did not). **`showIgnored = "hide"`** is the daemon's: ignored rows are left out of the listing the way dot-files are, and since a row is known to be ignored only once status has run, the view is rebuilt when it lands. It takes effect on the next listing of a folder, not retroactively.
- **git used in a terminal reaches kiki.** Nothing watched `.git`, so `git add`, a commit or a checkout left every badge and the branch chip stale. The watcher now holds two watches per repository being listed (`.git` and `.git/refs/heads`; at most 16 repositories, dropped once nobody lists under them), ignores git's `*.lock` files, debounces 300 ms, and re-runs status for every listing in memory under that root — each of which tells its windows `RepoChanged`. No listing is reset to do it. A repository whose status took over 2 s is skipped (`git::is_slow`, the flag that was set and never read), so a vast one is not re-run on every command. *Not watched:* `refs/remotes`, so ahead/behind after a `push` waits for the next change; branches with a `/` in their name.
- **Inspector**: state (with "· staged"), branch, and the last commit — short hash, author, relative date, subject — asked of `GitStatus` only for a row that is in a repository, and a late answer for the previous file is dropped.
- **Tests**: `tst_ColumnsPane` (three new), `tst_InspectorGit` (new), and the e2e flow **`git_status`** on a real repository: a state per row and a folder's aggregate; `git add`, `git commit` and `git checkout -b` from outside each arrive within the budget with a `RepoChanged` and no `Reset`; hidden ignored rows are absent.
- **For phase 9**: plan 15 loses the big-repository toast and gains the above; `gitState` IPC and the sidebar's dirty dot stay unbuilt — *not in 0.1.0* unless asked for. **The branch under a repository root was asked for (owner, 2026-09-21) and built the same day as a branch capsule beside the name** — see plan 15.

**Done 2026-09-19, fourth pass: launch and packaging — and a sort that could hang.** Suites after it: clippy clean; `cargo test` 151; QML 317; e2e 223 passed, 1 skipped.

- **The launcher was broken twice over, confirmed by running it** (the audit had it as probable). It looked for a running kiki with `qs -c kiki`, a named config the package never installs — "Could not find kiki config directory" — so it never found one and every launch was another window. And it passed the folder as an argument after `--`, which the shell never reads (it reads `KIKI_START`), so every launch — every `xdg-open` of a folder — opened home. Now: the instance is addressed by the path of its `shell.qml`, the way it was started (`qs` exits 255 when there is none); a second launch calls the new `shell present <uri>`, which opens it and raises the window (`hyprctl dispatch focuswindow pid:…`); a first launch sets `KIKI_START` and runs `qs -n`. Paths, relative paths and URIs are all taken. A first launch also starts the user's `kiki.socket`, because the package can only enable it from the *next* login. e2e flow **`launcher`** runs the real script: the folder opens in the running window, and `qs list` shows no second instance.
- **The Neovim preset's callbacks went nowhere** for the same reason (`qs -c kiki ipc call shell saved`). They go through the launcher now: `kiki --ipc saved %:p`.
- **"Show in folder"** (FileManager1): the window comes to the front; the file is selected once the folder has listed, found by the daemon (it used to look only among the rows already loaded, so in a big folder nothing was selected); `ShowItemProperties` opens the info panel, which it ignored.
- **Packaging**: `DBusActivatable=true` and `org.kiki.App.service` are gone (D12 — nothing owns that name); `kiki-portals.conf` is gone (never read on Hyprland, and it said something the first-run dialog does not); `kiki.install` no longer runs `xdg-mime default` or `systemctl --user` as root, and no longer claims kiki is the folder handler — that is each user's choice, made in the first-run dialog. The desktop entry names SMB and WebDAV. The chooser shows no remote Locations (D13; `tst_PortalPick`).
- **Found on the way: sort by size or date could hang.** A full `cargo test` failed one run in three on a sort that never replied — not load, and not from this pass (the commit before it has the same code). A row that scrolls out of every window before its stat job runs is skipped by that job, and stayed marked `queued` for ever; `enrich_all` passes over queued rows, so the listing never finished enriching and a `Sort` waiting on it never got its answer. In the app: scroll fast through a big folder, sort by size, and the sort hangs. One line (`stats.rs`), a deterministic test that fails without it, and 40 runs of the suite under load without a failure where it had been 1 in 24.
- **For phase 10**, on the installed package: a second `kiki` raises the first; `xdg-open ~/Downloads` opens Downloads; straight after install, before logging out, `kiki` finds its daemon; "Show in folder" from Firefox raises and selects.

**Done 2026-09-19, fifth pass: plan 29 F, and the editor's socket.** Suites after it: clippy clean; `cargo test` 152; QML 330; e2e 223 passed, 1 skipped.

- **Theme no longer polls (30 W4).** How Omarchy switches was read off `omarchy-theme-set`: it deletes `current/theme` and moves a new folder into its place — so a watch on any file inside is dead after the first switch, which is what the 2 s timer was papering over — and then writes `theme.name` **in place**. That file keeps its inode, so its watch survives every switch; it is now the only thing watched, and its changing re-reads the palette, the icon theme and the fallbacks. Proven on the real `FileView` with a scratch file: three in-place writes, three change events. The timer is gone. The files that need not exist are read with `printErrors: false` (also proven quiet), and the alacritty fallback looks beside `colors.toml`, where current Omarchy keeps it, before the old `~/.config` path. `tst_ThemeReload`.
- **The portal warning is not ours.** "Could not register app ID: Connection already associated…" appears in a twelve-line Quickshell script with no kiki in it. Qt's or Quickshell's; recorded, and to be dropped from plan 29's Verification.
- **Inspector's binding loop**: an image's height was bound to the picture's implicit size while the picture filled the box. The ratio is now *set* when the picture has loaded. `tst_InspectorImage`. (The warning could not be made to appear offscreen, so that test asserts the sizing and the reset, not the absence of the warning — look for it in a live log in phase 10.)
- **Video player**: a strip under a playing video — mute, a scrub bar that seeks on press and drag, and `0:12 / 3:40` — shown under the pointer or while paused. Mute is the panel's, so the next video starts the way the last was left. New `volume` / `volume-off` icons (added to `icons.js` by hand: the file says it is generated from `gen.py`, but `gen.py` no longer has `play` or `pause` either). Local files only, as before. *Still owed:* the no-sound report itself, which needs a video and ears — the code has sound on, and the default sink was HDMI.
- **`jobs::wait`'s 5 ms poll** is called only by that module's tests: marked `#[cfg(test)]`, no condvar. `Daemon.qml` no longer throws if a listing forgets to unbind.
- **The editor's socket**: `$XDG_RUNTIME_DIR` is already 0700, so the audit's worry was the fallback — with no runtime directory the socket went into plain `/tmp`, where any local user could connect and drive the editor as you. It is a directory of our own there now, made 0700 and refused unless it is a real directory, ours, and closed to others (a symlink or an open directory under that name is refused). A finished session's socket file is cleared, so the next `--listen` does not fail with "address in use".

**Done 2026-09-19, sixth pass — phase 2 is complete.** Suites after it: clippy clean; `cargo test` 155; QML 340; e2e 229 passed, 1 skipped.

- **Gallery.** A folder with no pictures is a folder to look through: the stage draws the selected entry with the artwork Icon view uses (`UI.KindIcon`), scaled to about two fifths of the stage (128–320 px), its name under it, and the filmstrip uses the same artwork; an empty folder says "Empty folder". After a delete the stage moves to what followed (`keepPlace`) — it used to jump back to the first picture, so deleting through a shoot restarted from the top each time. Type-ahead is off in the gallery. The bottom bar reads "7 of 31" there. A double click on a folder in the filmstrip goes into it (it was handed to an external app). **Corrections to the text below, from reading the code:** the arrows already step through *every* entry, not only pictures, and stay that way (so the count is of entries, which is also all the shell can know — rows are windowed and only the daemon knows every kind); `←` on the first entry going up to the parent is deliberate, and kept; `selectFirst` already fell back to the first row.
- **Columns drag**: nothing to do — a column holds one selection, so the row under the pointer *is* the selection.
- **`views.toml`**: an entry with `view = "mirror"` loses its `view` on load and keeps its sort and hidden-files choice; the file is rewritten once and never again when there is nothing to clean (the byte-identical assertion in `side_by_side` still holds). The 1,000-folder cap now has a test that exceeds it.
- **Two windows.** `FavoritesChanged` is sent — to every window — when favourites are set; `LocationsChanged` went only to the window that made the change and is broadcast now; a client that names another protocol version in `Hello` is refused with code `Version` and a sentence it can show. **`Gone` needed more than sending:** a cached listing holds its folder open, and while anyone does the kernel reports nothing for the folder's own removal (measured: only the `DELETE` of a file inside it; `DELETE_SELF` arrives after the last holder lets go; an empty folder vanishes without an event at all). So the watcher asks each watched path once a second whether it still leads to the folder opened there (`still_at`, one `stat`, at most 64 of them). e2e flow **`two_windows`**, with two real clients.
- **Project mode (D2)**, all four, none of them run by hand yet: Neovim's preset takes a folder (`e` on a folder did nothing, and the error was dropped — a tool that will not start now says so in a toast); each tool's terminal has a class of its own (`kiki-tool-<id>`), a window is matched by pid first and never given to two roles, and kiki's own window is found by its pid rather than by a class it does not have; the tree takes the keyboard focus while it is up, and `Ctrl+Shift+P` leaves as well as `Esc`; asking again for a terminal tool that is already running *in the same folder* returns that one instead of starting a second agent. `tst_ProjectTree`, `tree::tests`. **Owed to phase 10:** one hand pass on Hyprland — Arrange cannot be tested without a compositor.



Things that are wrong today. Each gets a test that fails first.

**Data and correctness**
- **Extract-undo** (`jobs.rs:625-646`) deletes every top-level name it extracted — including a folder that existed before and was merged into. Record what extraction *created* and remove only that. With D8 (extract into a folder named after the archive when it has more than one top-level entry; "Extract to…" asks where).
- **Archive traversal**: the `../` guard exists (`archive.rs:68-73`) and has no test. A crafted zip and tar; a typed error code rather than `Io`.
- **Job-role sessions** (½–1 day). `locations::resolve` always connects as `browse` (`locations.rs:224`), so mirror runs and everything in `transfer.rs` share the browser's session; plans 06 and 08 promise a job its own. `mirror::side_for` and `transfer.rs` connect as `job`, and the session is dropped when the job ends, however it ends. Test: cancel an upload mid-file, then list — the listing answers.
- **Cancel**: local `trash` and `delete` break out of their loop and return `Ok`, so a cancelled one ends `done` (`jobs.rs:549-551`, `578-580`).
- **Share**: the `kiki-share-<job>` temp directory leaks on every early return; a remote folder is `Read` as if it were a file; the fetch reports no progress (`share.rs:148-201`).
- **Search**: `capped` is true at exactly 10,000 hits; the scan stops at 40,000 *before* ranking, so the best match can be missed; a plugin that answers `Unsupported` (FTPS; SFTP without exec) gets no fallback walk, so location search silently finds nothing.

**Git (plan 15)** (1 day)
- Column view draws the state letter and dims ignored rows — the rows already carry `git`; drawing only. `tst_ColumnsPane` case, first column and one opened from it.
- **Nothing watches `.git/index`, `HEAD` or `refs/`**, so `git add` or `git commit` from a terminal leaves badges and the branch chip stale (plan 15's third Verification bullet fails). Watch them per repository root; re-run status for the listed directories under it after the 300 ms debounce; emit `RepoChanged`.
- `showIgnored` and `folders` have Settings UI and no effect. Wire them.
- Inspector: branch and last commit through `GitStatus` (D7).
- Amend the plan: the big-repository toast is dropped — `git()` forces `core.fsmonitor=` off on purpose, so suggesting it contradicts the code; `Status.slow` is removed or read.

**Launch and packaging** (½ day; verified on the installed package in phase 10)
- `packaging/bin/kiki` probes with `qs -c kiki` and starts with `qs -p …`: the probe can never match **(probable)**, so every launch is a new window. Same `-c kiki` in the nvim preset (`openin.rs:31`). Use one addressing; focus the existing window on a second open.
- Drop `DBusActivatable=true` and `org.kiki.App.service` (D12).
- `kiki.install`: `xdg-mime default` and `systemctl --user` run as root — they set root's handler and print a false message. Remove; first-run Integrate is what does this, per user.
- Delete `/usr/share/xdg-desktop-portal/kiki-portals.conf` — xdg-desktop-portal never reads a file of that name on Hyprland; Integrate's per-user `FileChooser=kiki;gtk` is the switch.
- Portal: hide Locations in the chooser (D13); `showItems` honours `properties` (turn the inspector on); raise the window on `ShowItems`.

**Plan 29 F**
- Inspector's `imageHeight` ↔ `height` binding loop (`Inspector.qml:112-118`).
- **Theme (30 W4)**: stop the 2 s poll — `theme.name` is the one trigger; prove its watch survives a second `omarchy-theme-set` first, else watch the directory. Read the three absent files quietly. `tst_ThemeReload.qml`.
- Portal "Could not register app ID": nothing in the repo registers one — it comes from Qt or quickshell. Find out whether it can be silenced; if not, record it as known and remove it from plan 29's Verification.
- `Daemon.qml:60`: guard the dispatch even though the cause is fixed.
- **Video player** (½ day): reproduce the no-sound report with the default sink on the speakers. Then: a mute toggle, a scrub bar, and position/duration. Local files only stays, and the still says so for a remote one.
- The nvim bridge's socket is created without a 0600 mode and is not removed when the session closes (`openin.rs:133`, `256-266`): anyone on the machine who can reach it can drive the editor. Mode it, remove it.
- `jobs::wait` on a `Condvar` instead of a 5 ms poll (30 W5).

**Views**
- Gallery (D6): `Del` advances to the next image rather than the first; "7 of 31 images"; type-ahead off; `←` on the first image stays put.
- **Gallery in a folder with no pictures** (½ day). Today the stage falls back to a 96 px stroke glyph (`UI.Icon`, `GalleryPane.qml:254-268`) — not the artwork Icon view draws — and `selectFirst()` and the arrow keys look only for `image` / `video` rows (`:45`, `:84-93`), so in a folder of documents nothing is selected and nothing can be stepped through. Instead:
  - The stage draws the selected entry with **`UI.KindIcon`, the same folder and file artwork `IconPane.qml:114` uses, scaled to the stage** — about 40 % of the stage's shorter side, clamped to 128–320 px — with the name under it. A thumbnail, where the row has one (a PDF, a remote picture), still wins over the icon. This replaces the stroke glyph for a non-picture entry in a mixed folder too, so the stage has one look.
  - With no picture in the folder, `selectFirst()` takes the first entry, the arrows step through **every** entry, and the filmstrip shows them all with their `KindIcon`s. `Enter` / `→` on a folder goes into it and stays in Gallery; on a file it opens it, as now. In a folder that has pictures the arrows keep stepping pictures only, as now — the fallback is for a folder with none, not a change to what Gallery is for.
  - "No pictures here" is kept for one case only: a folder with nothing in it at all (and then it says "Empty folder", like the other views). The status count reads "N items" when there are no pictures and "7 of 31 images" when there are.
  - `tst_GalleryPane`: a folder of two folders and three documents selects the first entry, shows a `KindIcon` of the scaled size on the stage, steps through all five, and enters a folder on `Enter`; a mixed folder still steps only its pictures; an empty folder says so.
- Columns: a drag carries the selection, not just the row under the pointer (`ColumnsPane.qml:329`).
- Project mode (D2, 1 day, all **probable**): the default editor preset is `accepts="file"` so `e` on a folder launches nothing, and the error is swallowed; editor and agent share the class `kiki-tool`, so Arrange cannot tell them apart; nothing takes key focus in the mode, so `Ctrl+Shift+P` may not leave it; the agent respawns on re-entry. Fix, then one hand pass.
- `views.toml`: drop `view = "mirror"` from entries on load, keeping sort and hidden (plan 29 J's last unbuilt item).
- Emit `FavoritesChanged` and `Gone` (D10); `Hello` checks the version (D11).

### Phase 3 — jobs that can be watched, and the orb (4½–5 days) — **in progress**

**Done 2026-09-19: the daemon's half — what a job says about itself.** Suites after it: clippy clean; `cargo test` 158; QML 340; e2e 237 passed, 1 skipped. Still to do: the job log, then the orb and the popup.

- **`Job::json` carries the table below**, as planned, with these differences. `phase` and `cancelling` are *derived*, not stored: a running job with no totals and no bytes yet is `preparing`; a job whose cancel flag is up and which has not ended is `cancelling` — and `cancel()` now broadcasts, so the view hears it at once rather than when the worker looks up. `set_totals` broadcasts. `isDir` for something on a server is not knowable when the job is submitted (a URI keeps no trailing slash); the transfer says once it has listed it. `current` and `rate` are only reported while the job runs.
- **A folder copy counts files.** It counted one per top-level item against a total of *files*, so a folder copy ended at "1 of 458". `ops::Progress` gained an optional per-file callback (`FileEvent::Start` / `Done`); the local copy counts files as they finish and names the one in hand. A job that ends `done` ends at its totals (a rename is one step however many files it moves).
- **Mirror runs count as they go** — every change used to be reported as "nothing done", so a run sat at 0 of 900 until the end — and name the item most recently begun. With several workers the file's own bar is left out (`size` 0): the bytes moving belong to all of them. The counts go on the job as `result`.
- **`chmod` and Empty Trash** report item by item instead of one jump at the end.
- **A transfer's error names the file** (`site/img/c.bin: Denied`, not `Denied`). A download that stops part-way can be undone (`undo_so_far`), and says where to `Reveal`.
- **`copy_file_range` in 64 MiB steps**, not 1 GiB: that step is how often a cancel is looked for and progress reported (this was phase 4's).
- **`ClearJobs`, `DismissJob { job }`** and the event **`JobsCleared { jobs }`**; live jobs are never forgotten. `Jobs` returns every live job plus the fifty most recent finished ones — "the last fifty of everything" let a long transfer fall off the list while it ran.
- **Hidden**: `_silent` ops, `movePairs`, `rmdirIfEmpty`, `chmodList`, `mirrorScan`.
- **Tests**: `jobs::tests` (three new); and in `remote_transfers`, against real SFTP and FTPS: an upload says it is an upload, of which file, which file is in hand and its size; it is `preparing` and then `running`; a rate appears; `cancelling` is said while it is still `running`.
- Also, from a loose end after phase 2: the shell greeted the daemon twice at start — the first time before its socket object existed, which logged "Hello failed" on every launch. One deferred greeting now.

**Done 2026-09-20: the job log.** Suites after it: clippy clean; `cargo test` 162; QML 340; e2e 253 passed, 1 skipped. Still to do in this phase: the orb, the popup, and the log window.

- **The first thing done was to listen and read**, as promised — a small upload to each real server with every library at `Debug` — and the two could not have been more different. **suppaftp's `Debug` is a story** ("Put file …", "PASV command", "Renaming … to …"), a few lines a file. **russh's is the wire** ("> msg type 94, len 128", "packet type 101"): 380 lines for six small files, a line or three per 32 KiB of a large one — it would flood the ring and cost a big transfer CPU. rustls writes six per FTP data connection. So the level is **per library**: `russh`, `russh_sftp`, `rustls` (and `tokio`, `mio`) are heard from `Info` up, where their warnings and errors are; everything else at `Debug` while a job's session is open, `Info` otherwise; never `Trace`.
- **Every plugin tells the same story, from the SDK**: `connect` and `disconnect` (with the role), `write path (n bytes)`, `read`, `mkdir`, `delete`, `rename a -> b`, and any refusal as a warning with its code. That is what makes an SFTP log readable now that russh's chatter is out — 26 lines for the same six files, where there had been 380 — and gio gets it for nothing. Listing and stat are left out: a browser does thousands.
- **Whose line**: the SDK stamps each with the role of the session its thread is serving; a job's sessions are its own (`job-<id>`, phase 2), so that is whose it is. A line from a library's background thread carries none and goes to every job with a session open on that plugin just then (`locations::jobs_using`). The browser's lines are nobody's job and live only in the plugin's own log — **`LocationLog { location }`**, for a location that will not connect.
- **Secrets are taken out in the plugin** (`sdk::liblog::redact`): after the FTP `PASS` command, and after `password` / `passphrase` / `authorization` / `secret` / `token` *when a `:` or `=` follows*. A sentence that merely mentions one — "password authentication failed for gideon", the line somebody needs — is left alone; the first version ate it.
- **Forwarding is asked for, not assumed** (`KIKI_PLUGIN_LOG`, set by the daemon when it spawns a plugin): `Log` events arrive between other frames, a binary stream's included, and the FTPS plugin's own test — a strict reader — found one in the middle of a download. A host that did not ask gets none.
- **kiki's own lines** open and close the log: what was asked and from where to where; each file as it is begun, with its size; cancel requested; how it ended, with counts; a failure with the file's name.
- **Kept**: a ring per job and per plugin — 2,000 lines or 256 KB, saying how many earlier lines it let go — read with **`JobLog { job, from }`** → `{ lines: [{ t, level, source, text }], next, dropped }`, asked again from `next`. Gone with the job (`DismissJob`, `ClearJobs`, the trim). A **failed** job's log is appended to `~/.local/state/kiki/failed-jobs.log`, begun again when it would pass 1 MB.
- **Tests**: `joblog::tests` (routing by role, the ring, the failure file); `liblog::tests` (redaction, both ways); and on real SFTP and FTPS — the log opens with what was asked and closes with how it went, names each file, holds the part file being written and renamed into place, on that job's own session, is under 150 lines, has no password in it or in the location's log, reads on without repeating, and goes when the job is dismissed.
- **Differs from the plan above**: no `req` id on a line (the role does the job); `Debug` is not blanket; the mock-server tests do not assert on library lines (they do not ask for them) — the real-server flow does.

**Done 2026-09-20: the orb, the popup and the log window — phase 3 is complete.** Suites after it: clippy clean; `cargo test` 162; QML 369; e2e 264 passed, 1 skipped. **`32-activity.md` is written and is the description of record; `activity-view-spec.md` is deleted**; `API-DAEMON.md` has the four requests and the event; plan 04's Activity paragraph points at plan 32.

- **`ActivityOrb`** (always there; dim / green and breathing / red with `!` until looked at), **`ActivityPopover`** with **`ActivityEntry`**, **`JobLogWindow`**; the unmarked 200 px click area is gone and the bar's status text makes room. What is shown and every word said is in `Kiki.Jobs` as pure functions (`shown`, `headline`, `statusLine`, `detailLine`, `completion`, `orbState`), with `Format.transferSize` and `Format.cleanError` — tested without a pixel drawn (`tst_ActivityText`), then on screen (`tst_ActivityOrb`, `tst_JobLogWindow`), then against the real daemon (e2e `activity`).
- **One-step jobs are not history**: `delete`, `chmod`, `trash`, `restore`, `mkdir`, `rename`, `emptyTrash` show while they run and if they fail, and go when they finish well — wider than the spec's "delete and chmod", for the same reason.
- **Looked at, not just tested**: the e2e flow saves `tests/e2e/out/activity-popup.png`. It showed two things no assertion had: **a failed job was offering Undo** — "Copy never-was.txt to dst · Undo" — because a partial inverse (phase 2) was journalled even when the job had made nothing; and its error read **"NotFound"**. Now a partial inverse is kept only for what really exists (`something_to_undo`), a job that stopped part-way says so in its toast, a local copy's error names the item as a transfer's does, and the daemon's bare codes are worded ("never-was.txt: not found").
- **The sidebar's location menu has "Connection Log…"**, for a location that will not connect.
- **Not built**: Save… in the log window (Copy all is); a mirror's per-file bar with several workers.



Plan 29 I placed the orb; decision 3 wants the spec's anatomy. The audit's finding is that **the daemon does not carry the data**: a job is `id, op, state, done, total, bytes, bytesTotal, title, error, undoable`, with a prose title and nothing about what is moving right now. So the daemon goes first.

**Daemon (1½ days)** — `Job::json` gains:

| Field | For |
|---|---|
| `name`, `count`, `isDir` | the entry's icon and header (the file or folder name, not "Copy 3 items to dst") |
| `src`, `dest`, `direction: "download" \| "upload" \| "local"` | "Downloaded 458 items" / "Uploaded 12 items" |
| `phase: "preparing" \| "running"` | the indeterminate bar. `transfer.rs` walks the whole tree before `set_totals`, so a remote job sits at 0/0; **`set_totals` must broadcast** (today totals arrive only with the next progress event) |
| `current: { name, bytes, size }` | the per-file detail row — set around `copy_file` in `transfer.rs` and `ops::copy_tree` in `jobs.rs`. With cumulative and per-file counters both sent, the spec's "detect the counter reset" trick is unnecessary |
| `rate` | bytes/s over a smoothed window, from the daemon, so every window shows the same number |
| `cancelling: true` | broadcast from `cancel()`; the header flips at once instead of when the worker notices |
| `result: { copies, deletes, skipped }` | the mirror's completion line, today only a toast string |
| `revealUri` | Reveal: the first local thing the job created |
| `hidden` | internal kinds (`movePairs`, `rmdirIfEmpty`, `chmodList`, `restore`, `mirrorScan`, silent undo deletes) stay out of the popup |
| the failing path in `error` | `VfsError::message()` gives a bare "Denied" for non-Io errors; plan 29 H needs the file named |

Also: mirror item counts move as actions finish (today `done` stays 0 until the end); `chmod` and `emptyTrash` report as they go rather than once afterwards; **`ClearJobs`** and **`DismissJob { job }`** — daemon-side, because `Jobs` is fetched again on reconnect and would bring dismissed entries back; `Jobs` never drops a live job out of its 50-entry reply. Rust tests for each.

**The job log (1–1½ days; D17)** — what the Log button opens.

- **SDK** (`kiki-plugin-sdk`, behind a `log` feature; one new dependency, the `log` crate, which is in `Cargo.lock` already through russh and suppaftp): a `log::Log` implementation installed by the SDK's `main` wrapper, so every storage plugin gets it without asking. Each record becomes a notification on the plugin's pipe — `Log { level, target, message, req }` — where `req` is the id of the request being served, held in a thread-local (and a tokio task-local for the SFTP plugin) set around each request handler. The gio plugin routes `glib`'s writer into the same sink.
- **Attribution**: the daemon knows which job issued `req`, and phase 2's job-role sessions mean a job's session serves that job and nothing else, so a line with no `req` — russh's background tasks, keepalives, a rekey — is attributed by *session*. Lines from a `browse` session belong to no job and are kept in a small per-location ring instead (the same window shows it from the sidebar's location menu: "Connection Log…"; that is the log one wants when a location will not connect).
- **kiki's own lines** go in the same log, so it reads as a story and not as a library dump: job started with its source and destination, each file begun and finished with size and time, each retry, skip and collision answer, the verify-before-delete of a move, cancel requested, the final outcome and error. Local jobs get these lines too — a local copy has a log, a short one.
- **Level**: `Debug` while a job session is open, `Info` otherwise; never `Trace` — russh's trace level is packet dumps, enough to slow a transfer and drown the rest. The logger's `enabled()` answers from an atomic, so a dropped record costs no formatting.
- **Secrets**: redact before the line leaves the plugin, not in the viewer — `PASS …` on the FTP control channel, anything following `password`, `passphrase` or `Authorization:`; a unit test feeds the redactor each. A log is something people paste into bug reports.
- **Storage**: a ring per job in the daemon — the last 2,000 lines or 256 KB, whichever comes first, with "… N earlier lines dropped" at the top when it wraps — kept as long as the job is kept and dropped with it (`DismissJob`, `ClearJobs`, the 100-finished trim). Not written to disk, except that a **failed** job's log is appended to `~/.local/state/kiki/failed-jobs.log` (capped at 1 MB, oldest out), so a failure from last night can still be read this morning.
- **Protocol**: `JobLog { job, from } → { lines: [{ t, level, source, text }], next, dropped }`, and `LocationLog { location, from }` of the same shape; while a log window is open it asks again from `next` on each `JobEvent`, so there is no second subscription to keep alive.
- **Log window** (`JobLogWindow.qml`): mono, timestamps relative to the job's start, level coloured (warnings yellow, errors in danger), library lines dimmer than kiki's own, follows the tail until scrolled up, a filter box, **Copy all** and **Save…**. Opened from the entry's **Log** button, and for a mirror from the Done screen beside "Save report…".
- **Tests**: the SDK logger against the stub plugin — a record inside a request carries its `req`, one outside carries none, `Trace` is never emitted, the redactor's cases; daemon — lines reach the right job with two jobs running on two sessions, the ring wraps at its cap and says so, a dismissed job's log is gone, a failed job's log is on disk; `mock_sftp.rs` and `mock_ftps.rs` each assert that a real transfer produces library lines *and* that the password appears nowhere in them; `tst_JobLogWindow.qml` — tail-following stops on scroll-up, filter, copy.

**Shell (2 days)** — `ActivityOrb.qml` and a rewritten `ActivityPopover.qml`:

- The orb exactly as plan 29 I: bottom right, in the window rather than the key-hint row; green and pulsing (`SequentialAnimation` on `opacity`, ~45 %–100 %, ~2.5 s) while anything runs; red with a white `!` when anything failed, outranking running, until the popup has been opened; dim and still otherwise, always present; tooltip in words. The 200 px click area goes; "N running ·" stays as text beside it.
- The popup anchored above the orb with its corner pointing at it; 400 px wide, height to fit, scrolling; header "Activity" with **Clear** (finished entries only); "No activity" when empty; closes on `Esc` and an outside click; a click on the orb within 250 ms of that close counts as the close.
- Entries from the spec: icon, bold header, 5 px bar (indeterminate while preparing or cancelling), dim status line, a disclosure that opens the current file's own bar and "12 KB of 254 KB (45 KB/sec)" once bytes move; when finished, a wrapping completion line. Entries keep their place — never re-sorted on finishing. Delete and chmod entries remove themselves on success. Buttons: **Log** on every entry, running or finished (opens the job log, above); **Cancel** while unfinished; **Reveal** for a finished transfer with a `revealUri` (selects it in the focused pane — kiki is the file manager); **Dismiss** otherwise. A failed entry's error line ends in "— see Log".
- Status lines by case — preparing; single file (bytes and rate); many files ("12 of 458 transferred — 2% complete"); mirror ("120 of 900 items · 40.2 MB of 1.10 GB · 2.1 MB/sec" with the current path and action); item-count jobs ("Processing 12 of 458 items"); cancelling. Sizes through `Format.bytes`, brought to the spec's rule (bytes under 1 KB, KB whole, MB one decimal, GB two). Error text with nested type prefixes stripped, "Failed" when empty — one pure function in `Format`.
- No poll timer: events arrive every 100 ms. Rebuild only while visible; hidden, update the model and the orb.
- Not applicable, and the new plan says so: the spec's child-job rule — a mirror is one job and per-action failures are "skipped".

**Tests**: `tst_ActivityOrb.qml` as plan 29 I lists it; `tst_ActivityEntry.qml` for every status and completion format, the disclosure, Reveal versus Dismiss, Clear leaving running entries, self-removing delete/chmod, hidden kinds absent, order stable; `tst_Format` for sizes and error stripping.

**Docs**: `32-activity.md` from plan 29 I plus the above; delete `activity-view-spec.md`; plan 04's Activity paragraph points at it.

### Phase 4 — large transfers (3½–4 days) — **done 2026-09-20**

**Done 2026-09-20.** Suites after it, all against real `sshd` and real `vsftpd`: clippy clean; `cargo test` 165; QML 369; e2e 278 passed, 1 skipped (`pointer_ops`); `remote_transfers` 117 of 117 against a vsftpd that demands TLS session reuse **and** against one that does not; `transfer_remote` 41 of 41 (5,000 files, 415 MB, up and down, both protocols); `transfer_local` 12 of 12, with two checks said rather than passed (a local copy is over in 0.3 s on a filesystem that clones).

**The rest of the phase, done the same day** — `remote_transfers`, per server: New Folder, Rename and Delete (a folder with things in it, and nothing beside it) as jobs; a name taken on the server asked about by name and answered each way; what time a file says it is. Two things came of it:
- **Keep Both had two names.** A local copy was given "a (2).txt" and a remote one "a copy.txt" — one choice, named by where the file happened to be. Both are "a (2).txt" now.
- **A file downloaded over FTP came down with the wrong time.** vsftpd has no `MLSD`, so the plugin reads `LIST`, which gives minutes for a recent file and only the *day* for one older than six months: a file from 16:53 arrived dated midnight. The server knows the second (`MDTM`, which is what `Stat` asks), so a download whose listing time is a whole minute asks once before stamping the file; a listing with real times almost never lands on a whole minute and is not asked again. Uploads: SFTP keeps the file's time; FTP cannot set one, is not asked to, and the test says so rather than pretending. *For phase 6:* the same coarse times reach the mirror's size-and-time detector on an FTPS download — `mirror_sftp`'s FTPS run must show a second run is a no-op.
- **…and the first fix for that broke the mirror's first rule, caught the same day by asking "are we doing what RelaySFTP taught us?"** Plan 08's engine is a port of RelaySFTP's, and its rule for times is in that engine's own comments: *"Stamp the source mtime onto the uploaded file so size+mtime detection stays idempotent"*, and on the way down *"stamp it back to the source so re-scans match"* — the time the **scan** gave, never a truer one, because the next scan will read the listing again. Asking the server for the exact second and stamping *that* on a mirrored file meant every old file on an FTPS server looked changed on every run. The exact time is now for plain downloads only (`ExecCtx::exact_times`, off for a mirror). **`remote_transfers` mirrors up and down against both real servers** with a file of twelve years ago, one of last year, one of this morning and one of this minute, and asserts that a second run finds nothing to do; with the mistake put back for the purpose, exactly one check failed — *ftps mirror download* — so the test is known to catch it.
- **The engine against plan 08's appendix (RelaySFTP's behaviour), checked line by line**: changed = size differs, or times more than 2 s apart (2 s itself is unchanged); an unknown time falls back to size; the clock offset is the median over same-size pairs and needs three of them, which is what absorbs a server whose LIST speaks in its own time zone; FTPS is size-only up (FTP cannot set a time, and is not asked to) and size+time down; SFTP size+time both ways; a job's sessions are its own and closed when it ends. **One thing was missing: the `mirror-mtime-skip` audit line** when a server refuses to take a time — the error was dropped, so a file that then re-copied on every run had nothing to explain it. Written now. *Not done, and not needed for 0.1.0:* the single-sided digest comparison (an object-store plugin's).
- **Still to be seen by hand (phase 8)**: a real FTP server in another time zone whose LIST omits the year for recent files — the listing parser's "a date in the future must be last year" step is suppaftp's, and only a round trip on this machine has exercised it.


**For phase 8, by hand:** one real FTPS server that is not vsftpd (ProFTPD or FileZilla Server) — the session-reuse fix is proven against vsftpd only. **For the release notes:** a name that is not valid UTF-8 cannot be transferred; a name with a line break cannot be stored over FTP. The harness now kills any server a flow did not live to stop (`KIKI_E2E_PIDS`): five `sshd`s had been left behind by runs that were killed mid-flow.

- **When part of a transfer goes wrong** (`transfer.rs`, and the same rule for local copies in `ops.rs` / `jobs.rs`): a file that cannot be copied is noted and **the rest goes on**; the job fails at the end saying how many and which ("1 of 6 could not be copied: site/index.html (permission denied)"), each one also in its log. A **move** deletes an item's original only when *all* of it arrived **and was checked** — the destination is asked its size (a `Stat` of the server's, the local file's length), not the copy's own count taken on trust; an item with anything missing keeps its original whole. An original that cannot then be removed is "copied, but the original could not be removed: …" — never a failure that implies nothing arrived. A cross-filesystem local move obeys the same rule. Tested on real SFTP and FTPS (14 checks) and in `jobs::tests`.
- **`kikid bench gen transfer`** (50,000 files in 2,000 folders eight deep, 2.5 GB in two large files), **`transfer-lite`** (a tenth: the default for the flows) and **`transfer-tiny`**; each with empty folders, a zero-byte file, a read-only file, a symlink, a name with a newline, a name that is not UTF-8, times spread over ten years, deterministic content that differs block to block, and a `MANIFEST.txt`.
- **`transfer_local`** and **`transfer_remote`** (up to the server, then down again — one flow, not two), on `transfer_common.py`: every entry arrives (compared entry for entry; large files and a sample of small ones hash for hash); progress never goes backwards and no total shrinks; it ends where it said it would; another folder still lists inside 500 ms while it runs; cancel mid-run ends `cancelled` promptly with no part file; the location still lists afterwards; undo of a large copy, and of a cancelled one, removes what it made and nothing else; the daemon's peak RSS and the throughput are recorded (`tests/e2e/out/transfers.json`). A check that could not have failed is **said, not passed** — a local copy of 415 MB is over in 0.3 s on a filesystem that clones, so "still responsive mid-run" prints a NOTE there. On timeout the flow prints the tail of the job's log and cancels, rather than walking away from a running job.
- **Measured, SFTP, `transfer-lite`** (5,000 files, 415 MB, loopback): up 73 MB/s, down 29–35 MB/s; the daemon grew **2 MB**; peak RSS 11 MB.
- **A name that is not valid UTF-8 cannot be transferred** — a transfer knows its files by text paths, so the odd bytes are gone before the file is opened. It used to fail as "NotFound"; it now fails by name saying why, and everything else arrives. **A limit of 0.1.0**, for the release notes; the listing itself shows such files. Byte-exact paths through `mirror` and `transfer` is the fix, and is not small.
- **FTP has no set-mtime, so it is no longer asked** on every file (`put`): one round trip fewer each.
- **The FTPS test server is `vsftpd`, and only `vsftpd`** (owner, 2026-09-20). It had been a Python one (pyftpdlib, from before this plan). Under a few hundred files in quick succession it stopped answering — even a fresh connection hung at "Reading server response…" — which the job's log showed at a glance and which had looked exactly like kiki hanging. `servers.py` runs the real thing as the launching user over explicit TLS with an RSA certificate. What that took, for whoever touches it next: **it exits with status 2 and not a word at an option it does not know** (`ssl_tlsv1_2`, `utf8_filesystem` in this build); **unprivileged, it cannot chroot**, so its "/" is the machine's and a flow addresses the fixture by full path, as over SFTP; **it cannot check a system password** (that is PAM's), so flows sign in to its anonymous account, still sending a password so that "no password in any log" tests something; an **ECDSA** certificate works under TLS 1.3 and leaves no common cipher under TLS 1.2, where vsftpd's default is `ECDHE-RSA-…`. `ftps_server.py`, the `e2e-servers` target and the venv are gone; with no `vsftpd` the FTPS pairs skip by name; CI already installs it. **With it, FTPS carries the same 5,000-file fixture as SFTP**: every check green, 58 MB/s up and 65 down without session reuse, 114 and 139 with.
- **Found by the large fixture: FTP command injection.** FTP is a text protocol and a command is a line. The fixture has a file called `line\nbreak.txt`; its name went onto the control connection as it was, became two commands, and left every reply after it answering the wrong question — fifteen innocent files failed after it, and the upload crawled through timeouts. A name reading `a\r\nDELE b` would have stored `a` and deleted `b`. **Nothing with CR, LF or NUL in it goes on the wire now** — paths in every operation, and the host, user name and password at sign-in (`on_the_wire`) — refused as `Invalid` before anything is sent. Such a file cannot be stored over FTP at all: it fails by name and the rest go on. `mock_ftps::a_line_break_in_a_name_never_reaches_the_wire` (nothing deleted, nothing stored, and the conversation still in step afterwards). SFTP is a binary protocol and carries such a name as it is.
- **Found by asking vsftpd to behave as it does by default: kiki could not use most real FTPS servers.** vsftpd (by default), ProFTPD and FileZilla Server refuse a data connection whose TLS session is not the control connection's own — "522 SSL connection failed: session reuse required". Under TLS 1.3 a ticket is used once and the control connection is given two, so **two files uploaded and the third failed**; rustls cannot offer a 1.3 ticket twice (the value is not `Clone`). A TLS 1.2 session can be offered as often as wanted — the long-standing way FTP clients satisfy such servers. So: a session store that keeps the FIRST session it is given, the control connection's, and hands exactly that to every data connection (`Held`), used over TLS 1.2 only. **Whether a server demands it is asked once, at sign-in, before any file moves** — a `LIST` of a name that is not there, one data connection, nothing transferred — because an upload cannot find out safely: vsftpd says "150, go ahead", takes the bytes, and only then answers 522. To make that one question decisive, resumption is *off* in the ordinary mode, so such a server refuses the first data connection rather than the third. Servers found to demand it are remembered for the life of the plugin process, so a job's own session signs in the right way at once; a listing or a download that meets a 522 anyway signs in again and retries (`opening`). `LIST`, not `NLST`: a server that did not know `NLST` left the client waiting for ever. The job's log says when it happens. **The fixture now requires reuse by default**, as vsftpd does, so this stays fixed; `KIKI_E2E_VSFTPD_NO_REUSE=1` for the other kind of server, which is run too.


Plan 29 H, less what exists. `remote_transfers.py` has the servers and the nine pairs; it has six small files and asserts none of H's list.

- **Move semantics first** (½ day; `transfer.rs:188-190`): a remote move deletes the original after the copy with **no verify step** — verify size (and mtime within the plugin's tolerance) before the delete. A delete half that fails reports "copied; the original could not be removed", never a failure that implies nothing arrived. **Decide stop-or-carry-on** when one file fails mid-copy (today: stop at the first error): carry on, fail the job at the end naming each file, as mirror does.
- Lift `Servers` (the user-mode `sshd` and FTPS fixtures, `add_location` with trust) out of `remote_transfers.py` into the harness.
- `kikid bench gen transfer` (½ day): ~50,000 small files in ~2,000 directories eight deep, two or three files of 1–4 GB, empty directories, a symlink, a newline and a non-UTF-8 name, a read-only file, a zero-byte file, mtimes across years. Today every profile's files are 1–6 bytes.
- `tests/e2e/transfer_common.py`: tree hasher (full for large, sampled for small); a `JobEvents` recorder asserting bytes monotonic, totals never revised down, an end at 100 %; cancel mid-run with no partial file under its final name; failure injection (the read-only file; `kill` the `sshd`); daemon peak RSS from `/proc`; throughput written in the bench JSON shape beside plan 26's baselines.
- `transfer_local` (same filesystem and across, reusing `cross_fs.py`), `transfer_download`, `transfer_upload` — out of the default run, by name, like `gallery_perf`. Each also asserts, with the job running, that another folder's listing answers inside its budget, and that the orb is green, goes red on the read-only file until the popup is opened, and that the popup's totals are the daemon's.
- **Expect these to break**: local copy uses `copy_file_range` in chunks of up to 1 GiB (`ops.rs:44`), so cancel and progress on a 4 GB file are coarse — cap the chunk (64 MB); remote `copy_file`'s partial-file cleanup on cancel has not been checked; a cancelled or failed big copy journals nothing, so its partial result cannot be undone — journal what was created. Half to one day is held for this.
- Extend `remote_transfers.py` (½ day): remote `delete`, `rename`, `mkdir`; collisions (replace, keep both); cancel; mtimes compared.

### Phase 4a — a folder that changes while it is open, and scrolling a long one (1 day) — **done 2026-09-20**

**Done 2026-09-20.** Suites after it: clippy clean; `cargo test` 168; QML 382; e2e 324 passed (the new flow **`listing_live`**, 12 checks, is in the default run), 1 skipped (`pointer_ops`); **`scroll_perf`** 32 of 32, by name. It began with the owner's question about the thumbnails that go away — *"if we are saving them, why would they go away?"* — and then *"isn't `Reset` heavy-handed?"*. The thumbnail files never went anywhere; what went was the listing's memory of them. Three changes were asked for, and the scroll test found a fourth.

- **A rescan keeps what the listing knew** (`listing/scan.rs`). `rescan()` carried metadata across by name and threw the rest away — `thumb.clear()`, `git.clear()` — so every rescan (a burst of changes, a job finishing in the folder, `Refresh`, a stale cached listing opened again) blanked every thumbnail and badge in view, to be asked for again one by one; and **nothing ran git status again afterwards**, so after a rescan the badges stayed gone until something touched `.git`. Thumbnails are carried by name now unless the scan says the file's time has changed; a thumbnail that *failed* is not carried (asking again is a lookup of the fail marker, not a decode); git state is carried and shown as it was while status runs again (skipped for a repository marked slow).
- **Pool indexes do not survive a rescan, and three things assumed they did.** A thumbnail job finishing after a rescan wrote its path to whatever file now had the old index — a picture wearing another's thumbnail — and now lands by name. `git_status` read names, ran, and wrote by index; it now gives up if a rescan happened in between (the rescan has started another). **And a stat job did the same with the lock held: in a folder that had shrunk, that is an index past the end — a panic that poisoned the listing's mutex**, found by the new test the first time it ran. Jobs carry `Inner::epoch` (one per rescan) and stop when it has moved on; an enrichment in progress is started again so a `Sort` waiting on it still gets its answer.
- **`Splice`: a file arriving or leaving is told as what happened.** `patch()` already spliced up to 64 changes into the daemon's own view in place — and then sent `Reset`, so every window threw away every row it held and asked again. It now sends `Splice { n, gen, ops: [remove pos | insert pos row] }`; `WindowCache` moves its rows, and **the selection, the row being renamed and each column's selected row move with them** (`Selection.splice`, `Pane`, `ColumnsPane.spliceColumn`) instead of being lost and found again by name. Still a `Reset`: more than 64 changes, a filtered view, a size or date order — exactly where the daemon rebuilds its view. A dot-file arriving in a view that hides them was being inserted into it; it is not, and is told to nobody.
- **`gen`: which state of the view an answer describes.** A `Window` reply and the event announcing a change travel separately and can arrive in either order — a reply computed *before* a splice arriving *after* it would put rows one position out, for good. Replies, `Reset` and `Splice` carry the listing's generation; the client drops a reply older than what it has been told (and asks again — at most five times, then believes it), and does not apply a splice the rows it holds already include. This race existed with `Reset` alone; it could not be seen because everything was thrown away so often. `Splice` is sent with the listing's lock held for the same reason.
- **Rows stay on screen across a `Reset`** (`WindowCache`). It emptied its rows and then asked, so every row and thumbnail blinked for a round trip — and nearly every `Reset` brings the same rows straight back. The rows held (less any past the new end) stay until the answer lands and are then replaced whole, so nothing stale outlives it. Sort, filter and show-hidden keep `Reset`: the window holds a few hundred rows of a folder that may have a hundred thousand, so there is nothing to reorder — the rows that belong on screen after a sort are ones it has never seen.
- **The fourth, found by the scroll test: a steady scroll was a scroll through blank rows.** `setViewport` *restarted* a 16 ms timer on every move; a scroll moves the viewport every frame, so at 60 frames a second the timer never fired and **no rows were asked for until the scrolling stopped** — 1 request in a 20-second scroll of 100,000 files, every frame blank, in all three views. Now: the timer is started, not restarted; **one `Window` is in flight at a time** and the next is asked the moment it lands, for wherever the viewport is by then (which paces the asking to what the daemon answers, not to the frame rate, and cannot pile requests up behind a slow server); **the look-ahead follows the speed** — 0.4 s of travel, between the old 200 rows and 3,000 — and what is kept behind scales with it; scrolling up asks for the part nearest the viewport first.
- **`scroll_perf`** (`tests/e2e/flows/scroll_perf.py`, by name, like `gallery_perf`; fixture `kikid bench gen flat100k`): List, Icon and Columns, each scrolled top to bottom twice through `UI.ScrollProbe` (IPC `scrollRun <ms>` / `scrollStats`) — 20 s, 5,000 rows a second, a pace a hand makes; and 4 s, 25,000 rows a second, a fling no hand makes. It reports frames, average / p95 / worst frame, frames over 33 ms, frames in which a row in view was not there, the time to fill in after stopping, and the requests made. Under `cage`, headless, software-rendered:

  | | before: blank frames | after: blank frames | frames over 33 ms, before → after |
  |---|---|---|---|
  | List, 20 s | 100 % | 0–1 % | 55 → 0–8 |
  | Icon, 20 s | 100 % | 0–2 % | 83 → 1–6 |
  | Columns, 20 s | 100 % | 0 % | 0 → 0 |
  | Columns, 4 s fling | 100 % | 1–2 % | 0 → 0 |
  | Icon, 4 s fling | 100 % | 6–57 % | — |
  | List, 4 s fling | 100 % | 8–72 % | — |

  On the owner's desktop (Hyprland, a GPU), the same day, driven by hand through the same IPC: 0 % blank at the 20-second pace in all three views, and in the fling 8 % (List), 10 % (Icon) and 1 % (Columns), with at most three frames over 33 ms in any run — the wide spread above is the software renderer's.

  The frame itself was never the problem — 16 ms on average in every view, before and after. **Open, and not a 0.1.0 matter:** the fling in List and Icon varies run to run and is bound by the window's own thread, not the daemon — each answer is 512 rows of JSON parsed and applied between frames in which forty full rows (`ListRow`: five columns, formatted) are being rebound, so the asking runs at 110 requests in 4 s where Columns, whose rows are a name and an icon, manages 230. The budgets in the flow are on the 20-second pace only (under 5 % of frames blank; filled in within 2 s of stopping; no frame a second long); the fling is printed. If it is ever to be closed: cheaper rows while moving fast (name and icon only), or rows delivered off the GUI thread.
- **Tests**: Rust — `a_rescan_keeps_the_thumbnails_it_had`, `a_thumbnail_that_lands_after_a_rescan_lands_on_its_own_file` (the one that found the panic), `a_small_change_is_spliced_not_reset`. QML — `tst_WindowCache` from 4 tests to 17 (rows kept across a reset and nothing stale surviving it; splice; the reply that arrives late and the one that arrives early; a missed step; a daemon stuck in the past; a steady scroll fed while it moves; one window in flight; an empty folder asked once), `tst_Selection` (two). e2e — `listing_live`, against the real daemon with real PNGs and the real watcher: arrive and leave are `Splice` with the right positions and nobody else loses a thumbnail; after `Refresh` *the very next* `Window` has every thumbnail; a changed picture is thumbnailed again and resets nobody.
- `API-DAEMON.md` has `Splice`, `gen`, and what a rescan keeps.
- **Every run of `scroll_perf` is recorded** (owner, 2026-09-20: *"record this per build, even if it is local and not in CI"*): one line of JSON appended to **`bench/scroll-history.jsonl`**, kept in the repository — the commit it was built from and whether the tree was dirty, the commit's subject, the machine and the renderer, the two paces, and each view's numbers. The run prints how it differs from the last one recorded on the same machine, renderer and paces (a software-rendered run and one on a GPU are not the same measurement, and are never compared). **`make scroll-perf`** builds, runs and records; **`make scroll-history`** (`tests/e2e/scroll_history.py`, `--all` for every machine) prints the record, oldest first. It fails nothing on a regression — the fling's spread under the software renderer is too wide for a tolerance to mean anything yet; once there are a few dozen lines, the 20-second pace can be given one, like `kikid bench compare`. `KIKI_SCROLL_HISTORY` moves the file, or set empty records nothing.

### Phase 4b — decorations, and a thumbnailer of its own (1 day) — **done 2026-09-20**

**Done 2026-09-20.** Suites after it: clippy clean; `cargo test` 172 (plus one `--ignored`, below); QML 385; e2e 329 passed (`listing_live` grew to 17), 1 skipped (`pointer_ops`). The owner's idea, after 4a: *"for the thumbnail, git, etc… would it make sense to break them out into a new decorator class? main daemon just worries about file handling then. decorator could be another process."* Taken in two halves, because they are two different arguments.

**The decoration layer (`listing/deco.rs`).** `thumb: HashMap<u32, String>` and `git: HashMap<u32, Entry>` were two maps keyed by **pool index**, filled by two hand-wired workers with a queue flag each and a line each in `rescan`. A pool index means something only inside one scan, and every 4a bug was that one fact: a thumbnail written onto whatever row had taken its index, a badge on the wrong row, and a stat job indexing past the end of a shrunk folder — a panic with the listing's lock held. They are now one `Decorations`, **keyed by name**, and the rules live in one place:
- `Thumb::At { path, mtime }` is only *shown* for a row whose time still matches, `Thumb::None { mtime }` is an answer ("nothing to be made") that stops it being asked again, and `Thumb::Asked` is in flight. So "the file changed, ask again" is one comparison, not a rule copied into three places.
- **`rescan` no longer carries anything.** It keeps metadata by name as before and calls `retain_names`; decorations survive by construction. The stale-thumbnail check that 4a added there is gone — the mtime rule above already covers it.
- `git_status` writes by name, and the view's hide-ignored filter asks by name.
- Cost: a hash of the name where there used to be an array index, once per row of a window. Names are stored only for rows that know something.

**The thumbnailer is a process (`thumber.rs`, `bin/kiki-thumber.rs`).** The argument is not tidiness; it is that **thumbnailing is the only place kiki decodes a file's contents**. Everything else it reads it wrote itself or asked the kernel for. Even for video and PDF, where `ffmpeg` and `pdftoppm` are already subprocesses, their output came back through `image::load_from_memory` *inside the daemon* — so a malformed file could take down every window's listing and every running job. It now takes down one child that is started again.
- **It speaks the plugin framing** (`Plugin::spawn_path`, which exists for exactly this: a binary that speaks the framing and nothing more). It is *not* a location plugin — no `Describe`, no `Connect`, no location of its own, and the SDK's `Handler` would only have given it a scheme and a role it never uses — but there is no reason for the daemon to own a second way of talking down a pipe. **Decided (owner, 2026-09-20)** after the first cut did own one: *"I thought for the thumber we were going to make it a plugin?"* — which is what had been said, and was not what had been built.
- `{"id":7,"type":"Thumb","uri":…,"kind":…,"size":128,"mtime":…}` → `{"id":7,"ok":{"path":"…png"}}`. **The pixels never cross the pipe**: the child writes the cache file and the freedesktop failure marker and answers with a path. "No thumbnail for this one" is an `ok` with a **null path, never an `err`** — an `err` reply and a death arrive as the same `Err`, and the daemon has to tell "this file has no picture in it" from "this process just died on it": one is forgotten, the other is a strike. The first cut told them apart by asking the child whether it was still alive, which is a race with the kernel and got the count wrong the first time the test ran.
- **A cache hit never leaves the daemon**: `thumbs::lookup` reads a PNG we wrote, so the common case costs no pipe and no process.
- **The workers are what limit how much is in flight** — four threads, one blocking request each. Not throughput: it is what lets "twenty seconds with no answer" mean *stuck* rather than *busy*.
- **A child stuck on one file for 20 s is killed** (`Plugin::request_within`, added here: a transfer may fairly take the host's two minutes, a thumbnail may not); **a file that kills it twice is given up on** (the first death is forgiven: it may have been the machine, or the file beside it). That is what stops a crash becoming a loop of restarts. What the plugin host has — framing, the reader, the pending map, request/reply — is used as it is; what it has no reason to have is the supervision above, and that is all `thumber.rs` is.
- `preview.rs` (the inspector's large preview) and the `Thumbnail` request go the same way; the daemon no longer calls a decoder at all.
- **Packaged**: `usr/lib/kiki/kiki-thumber`. `kikid` looks beside itself first, then there, then the path. Absent, it says so once and answers every request "no thumbnail" rather than leaving rows waiting for ever.

**Two bugs the tests found, both in the part that exists to be safe:**
- **Killing a child left it alive.** The thumbnailer starts `ffmpeg` and `pdftoppm`, which inherit its stdout. Killing the child alone left a grandchild holding the write end of the pipe, so the host never saw it close and waited on a pipe that would never shut — in exactly the case the watchdog exists for. **Fixed in `plugin.rs`, for every child kiki runs**: each is given a process group of its own at spawn (`pre_exec` / `setpgid`), and `kill_group` takes the group down. It was latent for the location plugins too — gio reaches gvfsd — and would have been a hang nobody could explain.
- **A hang was counted as two deaths.** The first cut let its own kill come back as a second death, so one hang burned both of a file's chances and the second child was never given its 20 s. Visible in the test's own clock: the hang test now takes 40 s, which is the two watchdog periods it should always have been.

**Tests**: `a_file_that_kills_the_decoder_costs_only_itself` (a stand-in thumbnailer that speaks the framing and dies on a named file: the file is retried once, then given up on, and the files beside it are answered as if nothing happened); `a_decoder_that_hangs_is_killed` (`#[ignore]` — it waits out two 20 s watchdogs; `cargo test -- --ignored`. Its stand-in leaves a child of its own holding stdout, which is what a wedged `ffmpeg` looks like, so it is the process-group fix that it tests); `no_thumbnailer_answers_rather_than_hangs`; `a_remote_file_is_refused_without_asking`. In `listing_live`, against the real daemon: the thumbnailer is a separate process, it is `SIGKILL`ed mid-flow, the daemon and the folder carry on, what was made is still shown, and the next picture is thumbnailed by a child that was started again.

**Asking for far less, after the benchmarks were pointed at it (owner, 2026-09-20).** Running `scroll_perf` and `gallery_perf` against the new process turned up three faults, each found only because the work had been moved somewhere it could be counted:

- **Thumbnails were asked for the whole *held* range, not the part on screen.** A window holds a viewport plus the look-ahead it scrolls into — during a fling, up to 3,000 rows — and every thumbable row in it was asked for. That is a `pdftoppm` or an `ffmpeg` spawned for a row nobody is looking at. `Window` now carries **`viewFirst`/`viewCount`** beside `first`/`count`, `Subscriber` keeps both, and thumbnails are made for `covers_view` only (omit them and the whole range counts as on screen, which is what a client that does not say gets). Measured directly: a 512-row window with 40 rows on screen asks for **40** thumbnails, not 512.
- **A queued thumbnail nobody can see any more is dropped unstarted** — the owner's ask. `Job` carries a `wanted` predicate, checked when a worker picks the job up rather than when it was queued, and `Answer::Dropped` is **not** `Answer::None`: "there is no thumbnail for this file" must stop the row asking again and "nobody is looking at that row" must not, so a drop calls `unask_thumb` and the row asks afresh if it is scrolled back to. *On its own it made scrolling worse* — 0.1 % → 12.6 % blank frames — because clearing the backlog let the workers reach live rows and the thumbnailer then spawned 19,000 decoders during one scroll, against 2,700 before. It is the pair that works: with the viewport change the queue never grows, and the same scroll came back at 0.0 % blank with no frame over 33 ms.
- **A thumbnail asked for before the row's stat landed was made twice.** `window()` skipped a row with no metadata only if its stat was not *already* queued; otherwise it asked with a modification time of **zero**. A thumbnail is cached at `md5(uri).png` and keyed by mtime, so that one was thrown away the moment the real time arrived and made again over the top of itself — twice the decoding, and the same file on disk both times. Found because `gallery_perf` counts cache *files*: about fifteen pictures were made twice, the count never reached what was asked for, and the check read **3/s** where the thumbnailer was really doing **307/s**. It now waits for the stat; `run_stats` asks as soon as that lands. `gallery_perf`: 486 thumbnails in 1.6 s, 11 of 11 checks.

**What the fling still is, measured properly (owner asked what could be done about it).** The
ceiling is arithmetic: **one `Window` in flight, 512 rows an answer**, so what a client can take in
is `512 / round-trip`. Measured over the 4-second fling — which is 100,000 rows, or 25,000 a second:

| view | answers in 4 s | round trip | rows/s it can take | keeps up? |
|---|---|---|---|---|
| columns | ~205 | 20 ms | 25,600 | yes |
| icon | ~147 | 27 ms | 19,000 | no |
| list | ~125 | 32 ms | 16,000 | no, by about 2:1 |

**The round trip is not the daemon's** — it answers a 512-row window out of memory. It is the window's
own thread: the IPC, parsing the JSON, `_apply`, and then every delegate rebinding. What differs
between the three views is exactly the delegate — a column's row is a name, an icon is artwork, and
a `ListRow` is five formatted columns — which is the order the numbers come in.

- **Done: ask about where the screen is going, not where it has been.** `_fill` anchored every
  request at the first missing row, which during a fling is the hole *behind* the viewport — rows
  nobody will look at again. It now predicts (`viewportFirst + velocity × round-trip`) and asks
  there, but **only when the screen is outrunning the answers** (`lead > maxRequest`), so a view
  that is keeping up is not perturbed into overshooting. Unit-tested both ways.
- **Not claimed: that it moved the number.** Three runs a variant, medians `list` 53 % → 46 % → 59 %
  and `icon` 50 % → 47 % → 47 %, with each variant's own range spanning twenty to thirty points. The
  measurement cannot separate them, and this is written down rather than reported as a win. A
  batched `_apply` prune was tried on the theory that `delete` deoptimises the row object, could not
  be shown to help either, and was taken out again: an unproven change is not worth its risk.
- **A "lean row" was proposed and then measured, and the measurement says not to build it.** The
  idea was to send name and kind only while a view is moving fast, against the present name, kind,
  isDir, isLink, size, three times, mode, owner, group, digest, opened, thumb and git — a quarter of
  the bytes, and worth 2–4× if parsing were the cost. `ScrollProbe` now reports where an answer's
  time actually goes (`waitMs` / `storeMs` / `bindMs`, in the history file from here on):

  | view | pace | waiting for the answer | storing it | delegates re-reading |
  |---|---|---|---|---|
  | list | 20 s | 19–22 ms | 5 ms | 0.9 ms |
  | list | fling | 15–18 ms | 1.8 ms | 3.0 ms |
  | columns | 20 s | **7 ms** | 4 ms | 0.3 ms |
  | columns | fling | 13–15 ms | 2.6 ms | 0.5 ms |

  The daemon does the same work for both: the same listing, the same 512 rows. So **columns' 7 ms is
  what an answer costs when the window's thread is free** — the daemon, the socket, and turning the
  reply into JavaScript — and list's extra ~13 ms is the answer sitting in the queue while that
  thread renders. Storing the rows is small and the synchronous `rowsUpdated` is almost nothing,
  because QML bindings are lazy: a `ListRow`'s five columns are not evaluated there but in the
  render pass, which is the very thing holding the answer up.

  So **about two thirds of a round trip is the window rendering, not the data**, and a lean row
  attacks the other third. It would still help a little — fewer bytes to convert, and empty columns
  are cheaper to lay out — but nothing like 2–4×, and not enough to close a 2:1 gap. Reaching for it
  first would have cost a day for a fraction of what it looked worth.
- **What would actually matter**, if this were ever worth fixing: fewer delegate updates per frame
  while the view is moving — a `ListView` scrolled 400 rows a frame rebinds every visible delegate
  every frame, and each of those is five formatted columns. That is a rendering decision about what
  a flung list should show, not a protocol one, and it is not 0.1.0's.
- **And it is a stress figure, not a a user one.** 25,000 rows a second is 400 rows a frame: nothing
  on that screen can be read. The pace a hand makes — 5,000 rows a second, itself very fast — is
  **0–2 % blank in every view**, every run. That is what the budgets assert; the fling is recorded.

**Not done, and deliberately**: git stays in the daemon — `git()` already shells out, so the slow and risky part is in another process already and moving the orchestration would buy nothing. `meta` and `opened` stay for the same reason in reverse: a `statx` and a local lookup cost less than the pipe would. **Post-0.1.0**: a real sandbox on the child (seccomp or Landlock: no network, no write outside the cache) — the process boundary is what 0.1.0 buys, and the confinement is the next step, not this one.

### Seen while running the app (owner, 2026-09-20) — **closed 2026-09-20**

- **Gallery: thumbnails turn into generic file icons — found, 2026-09-20.** It was **remote folders**: `Uri::to_path` drops the scheme and the host, so the daemon thumbnailed a remote picture by reading **this machine's** copy of that path. Nothing is there, usually — so a fail marker was written against the remote name (73 of them in the owner's cache, all `sftp://ghs/...`), and since a marker is only forgiven when the file's mtime changes, that picture never got another chance. Where something *was* there, the remote row wore a local file's picture. Neither the SFTP nor the FTPS plugin implements the SDK's `thumb` hook (only PTP does), and the daemon never sent `Thumb` anyway, so remote thumbnails had never once worked.
  - **Decided (owner, 2026-09-20): thumbnails are `file://` only for 0.1.0** — *"we don't want thumbnails for remote items for now … just file://"*. `thumbs::generate` refuses a non-local URI (a *cached* one is still served, so nothing already made is lost), and a remote listing does not queue the jobs at all (`Listing::thumbable`). A remote picture shows its kind artwork, and the gallery's stage names the file under it. Fetching the bytes to thumbnail them — the plugin's `Thumb`, or a capped `Read` — is post-0.1.0, and the SDK hook is already there for it.
  - **The local half was phase 4a's**, and is fixed: a thumbnail job finishing across a rescan used to write to whatever row then held its index (and left the right row marked "queued", so it was never asked again), and nothing was fetched at all while a view scrolled. Confirmed by hand on 2026-09-20: `~/Pictures` in the gallery, a file touched under it to force a rescan, every thumbnail still there.
  - **Tests**: `a_remote_picture_is_not_thumbnailed_from_a_local_path` (Rust); `tst_GalleryPane` gains the filmstrip's three — pictures kept across a reset, a thumbnail arriving late reaching its tile, and a remote picture showing kind artwork. All three fail if the strip's delegate stops re-reading its row, which is what the old note suspected and could not confirm.

### Phase 5 — drag and drop (2½ days)

**What is there** — read out of the code on 2026-09-20, because the note that stood here was wrong in two places. All four views take drops: each has a background `DropArea` and folder rows have their own. **List and Icon are sources that carry the selection** (`Pane.dragUris`: the selection when the pressed row is in it, that row alone when it is not). **Columns is a source that drags one row** — `ColumnsPane.qml:348` builds its `text/uri-list` by hand instead of asking `Pane.dragMime` — so "Columns carrying the selection is in phase 2" was wrong: phase 2 did not do it, and it is listed below. `Pane.dropAction` decides — `Ctrl` copy, `Shift` move, move within one scheme **and host** (`placeOf`), copy across — and already refuses an item into the folder it is in, a folder onto itself, and a folder into something inside it. `Pane.dropInto` makes one drop one job and guards the stacked drop areas against handling it twice. Remote ends work. Favorites takes drops as "add a favourite" (`Sidebar.qml:65-75`). `tst_DropAction` (10 cases) and `tst_DragDrop` (6) cover the table.

**Decisions taken (owner, 2026-09-20)**

- **Link is not in 0.1.0** — struck, not deferred to late in the phase. `Ctrl+Shift` link and the menu's "Link here" go. The daemon has no `link` operation at all (`jobs.rs` knows `copy`, `move`, `trash`, `delete`, `chmod`), so this is not a QML change but a new op with its own undo, journal entry and tests — and a symlink is the least of what a file manager owes at a tag. **Plan 29 is amended in phase 9**: its drag table loses the `Ctrl+Shift` row (line 222), `Alt`'s menu becomes Copy here / Move here / Cancel (223), and its `tst_DragDrop` list loses "link offered only local → local" (252).
- **A refused target is only what the code can know.** Plan 29 wants the "no" cursor for "a read-only location", but no location carries such a flag and nothing asks a plugin whether it may write. Refusal during a drag therefore means what `dropAction` already computes — a folder into itself, an item into the folder it is already in. A read-only location waits for the flag, post-0.1.0.
- **A server's files dropped on the Trash are deleted, after a question** — *"dragging a remote item to trash should just delete the remote item, telling the user it's permanent with a dialog"*. Not a refused target, then, which is what this list said before.

**To build, in this order.** The first comes first because it makes everything after it testable the day it is written.

1. **IPC `shell drop <uris> <dest> <modifiers>` — done 2026-09-20.** It hands `Pane.dropInto` an object shaped like the `DragEvent` Qt would give it, so everything a drag does after the button goes down is what runs; it answers what the drop decided (`{accepted, action, items}`) so a flow need not guess. **The URIs go newline-separated, as a `text/uri-list`, not as JSON: Quickshell's IPC strips square brackets out of an argument** — `["x"]` arrives as `"x"` and `[]` arrives as nothing at all — which cost an hour and is worth knowing before anyone reaches for JSON in an IPC argument again. **`tests/e2e/flows/drag_between_panes.py`** (in the default run): every local / SFTP / FTPS pair in both directions and each to itself, plain / `Ctrl` / `Shift` / a multiple selection, asserted on disk, plus the three refusals — **81 checks**. A failed check prints what the daemon said went wrong, because a drop is a job and a job that fails says why.
2. **Trash answers for remote files before the job, not after — done 2026-09-20.** The sidebar's Trash used to submit `trash` for any URL it was handed, and `jobs.rs:719` refuses a remote `trash` (*"a server has no trash: use Delete (Shift+Del)…"*) — nothing lost, but the user was told after the fact, by a failed job. Now `Ops.trashSelection` sorts what it is given: this machine's files go to the trash as ever, without asking; a server's get *"Delete permanently? — a.txt is on homelab, which has no trash. It will be deleted for good, and this cannot be undone."* in danger colours, and a `delete` job on Yes. **`Del` and the menu's Move to Trash follow the same rule (owner, 2026-09-20: "do Del too")** — the drop, the key and the menu are one function, so they cannot drift apart. The daemon's refusal stays, as the guard for anything that sends a remote `trash` anyway. **Tests**: `tst_Ops` — local goes without asking, Del in a remote folder asks then deletes, a dropped remote file asks then deletes, declined does nothing, a mixed drop does both; `drag_between_panes.py` drops on `trash:///` from every end — local into the trash, remote asked, No left alone, Yes deleted on the server and nothing in this machine's trash — then presses Del on a file in each server's folder — **98 checks** now. Two IPCs make that possible without a keyboard: `shell question [yes|no]` reads the yes/no dialog and answers it, and `shell action <id>` runs a keymap action (`trash`, `deleteForever`, …) as its key would.
3. **Gallery as a drag source — done 2026-09-20.** Two sources, both what `ListRow` does (`Drag.Automatic`, `pane.dragMime`, threshold 8) through one `GalleryPane.dragFrom`: **the picture on the stage** — the thing a gallery is for dragging out — and **each filmstrip tile**, which becomes the picture on show as it goes, the way a dragged row becomes selected. A selection carries whole. **The stage drags only while there is nothing to pan** (`canPan`): a picture zoomed past the stage is moved around by dragging it, and that has to keep working. **Tests** (`tst_GalleryPane`, five): the picture on show leaves as a `text/uri-list`; a selection leaves whole; a tile pulled up out of the strip, and one pulled along it, each start a drag carrying their own picture; a zoomed picture pans and starts no drag — checked to fail with the `canPan` guard taken out. They press and move a real pointer in the test window: the offscreen platform ends a drag the moment it begins, so what is asserted is that one began and what it carried. `gallery_perf` run against the real shell afterwards, clean.
4. **Columns drags its row — done 2026-09-20, and the item's premise was wrong.** "Columns drags the selection" assumed a selection that columns view does not have: each column keeps one highlighted row of its own (`columns[i].selected`) and there is no way to pick several — no `Ctrl`, no `Shift`, and `pane.selection` is not what a column draws. So a columns drag carries the row pressed, which is all there is to carry. What changed: the drag now **marks that row as it goes** (as List and Icon select a dragged row — what moves and what the column shows as chosen are the same row; marked, not pushed, so a drag opens nothing), and the payload comes from **`Pane.uriListMime`**, which `dragMime` now uses too, so the shape of a `text/uri-list` is written once. **Tests** (`tst_ColumnsPane`, two, pressing and moving a real pointer): a dragged row carries its URI, becomes the marked row and opens nothing; a row in a *deeper* column carries that column's folder's URI, not the pane's. Multi-select in columns is a feature, not this item, and is **not in 0.1.0** unless the owner says otherwise.
5. **`Alt` and a right-button drag — built 2026-09-20, REMOVED 2026-09-21 (owner).** *"what is the drop menu? we already have a context menu"* … *"remove the tooltip and drop menu."* The Copy here / Move here / Cancel menu is gone, with everything that served it: the `application/x-kiki-ask` mark a right-button drag carried, `Pane.askDrop`, `Shell.dropMenu`, `Link` among the sources' supported actions, and the `menuClick` IPC. **What is left**: a right-button drag is an ordinary drag (Qt starts one with either button — kept as a test in `tst_ListPane`; whether Hyprland carries it is a phase 10 line), and **`Alt` copies** — not by design but by Qt, which falls back to Copy for a source that offers no Link (measured, and pinned in `tst_RealDrag`). **Plan 29 K is amended in phase 9**: the `Alt` row and the right-button row leave its drag table. A move between machines by dragging is therefore not possible — Shift reads as no key (item 6) — and cut and paste is how it is done.
6. **Shown while dragging — done 2026-09-20; the hint REMOVED 2026-09-21 (owner); and it found that the keys had never worked.**
   - **The finding.** Every drop read `drop.modifiers`, and every test built its drop by hand with a `modifiers` field in it. **A real DragEvent has no such property** — Qt's own metatypes list `x, y, source, keys, supportedActions, proposedAction, action, accepted, hasColor, hasHtml, hasText, hasUrls, colorData, html, text, urls, formats` and nothing else. So in a real drag `drop.modifiers` was `undefined`, every drop took the rule by place, and **Ctrl and Shift had never once reached the code**; 81 checks in `drag_between_panes.py` and the `tst_DropAction`/`tst_DragDrop` tables passed on a field that does not exist. Plain drops were right all along, which is why nobody noticed.
   - **What Qt does instead, measured.** Qt folds the keys into `proposedAction`. Under the `minimal` platform Qt performs a real in-process drag, so a test can hold a key and look (`tests/qml-drag`): **no key → Move** (the source's own `Drag.proposedAction`), **Ctrl → Copy**, **Shift → Move**, **Alt → Copy** (Link, were it offered). No key and Shift cannot be told apart, and no value the source proposes gets round that (`TargetMoveAction` is folded back to Move).
   - **So the keys mean** (`Pane.wantsCopy`): **Copy copies** — Ctrl. **Move follows the rule by place** — no key or Shift. The one loss is **Shift can no longer force a move between machines**; it copies, as no key does. Pairing it the other way — the source proposing Copy — would have lost Ctrl within one disk instead, turning a copy the user asked for into a move: the worse way round.
   - **The cursor is the feedback, and only the cursor** (owner, 2026-09-21: *"don't we have custom cursors for all the drag types? copy adds a plus, denial a not allowed icon"*). kiki has none of its own and needs none: the cursor theme has them, and Qt picks one from the action the drop target reports — which until now nothing reported, so inside kiki the cursor said *move* everywhere. Every view's drop areas are one component now, `views/DropTarget.qml`, which asks `Pane.dragOver` on every enter and move and tells the drag Copy, Move, or **Ignore for a refusal — the "not allowed" cursor *during* the drag**. A refusing target still takes the drag (a DropArea that turns one away hears no more of it), so the outline a folder draws binds to `welcoming`, not `containsDrag`; and a refused **drop** is answered with Ignore rather than passed on, so the folder behind cannot quietly take what was just refused. **The one-line hint by the pointer was built and taken out again** — it said what the cursor says. Plan 29 K loses it in phase 9.
7. **Focus and cancel — done 2026-09-20.** A drop hands the focus to the pane it landed in (`Pane.received` → `Shell.focusPane`). The press that starts a drag keeps the selection it is dragging (pressing one row of several drags them all). **Escape** is Qt's: it cancels the drag and nothing is dropped. *Seen under `minimal`, to be looked at on Hyprland*: after Escape the DropArea is never told the drag has gone — `containsDrag` stays true — so a folder's drop outline may stay lit until the next drag passes over it. *The press focusing the pane it is in* is PaneFocus's rule for every press (plan 23, 2026-09-19) and is left as it is: after a drop the receiver has the focus either way.
   - **Tests.** **`tests/qml-drag/tst_RealDrag.qml`, 11 real drags** under `minimal` (part of `make test`): no key within a disk moves, and the folder showed itself welcoming while the drag was in the air; Ctrl copies; Alt copies; Shift within a disk moves; **a key pressed mid-drag decides the drop** — read at the drop, not captured at the start; a refused target shows refusing during the drag and takes nothing; across machines no key and Shift both copy; the receiving pane says it received; a pressed row of a selection carries the whole selection; Escape drops nothing. The hand-built events in `tst_DragDrop`, `tst_DropAction` and the IPC (`Shell.fakeDrop`) are shaped as Qt shapes a real one now — `proposedAction`, no `modifiers` — so the unit layer and the e2e flow test what a hand does; `drag_between_panes.py` has Shift following the rule by place and the focus going to the receiving pane side by side.

- Favorites stays "drop adds a favourite"; plan 29 K's "favourites are drop targets for files" is wrong about the code, and is amended.
- **Not in 0.1.0**: link (above); spring-loading — no hover-to-open, no `springDelayMs`, no `tst_SpringLoad`.

**Seen while building it, and not chased**: `Stat` on a remote **directory** answers `NotFound` — for SFTP and for FTPS alike, so it is the daemon's or both plugins' and not one server's. Files stat fine and listing a folder is unaffected, so nothing in phase 5 needs it; but the inspector stats whatever row is selected, so a remote folder's inspector is worth a look in phase 6 or 7.

**Found while building item 4 and fixed the same day: in Columns the keyboard acted on the wrong row.** `ColumnsPane` never touched `pane.selection` — it keeps its own highlight per column — while `Del`, `Shift+Del`, `Ctrl+C`/`X` and the rest went through `Ops.selectedUris()`, which reads `pane.selection` over the pane's own listing. **Reproduced end to end, 2026-09-20**: choose `a.txt` in list view, switch to Columns, move the highlight to `b.txt` (the inspector showing `b.txt`), press `Del` — **`a.txt` went to the trash**. With nothing selected before the switch, `Del` in Columns did nothing at all, which is how it stayed quiet.

**The rule, from the owner (2026-09-20)**: *"as we drill down, the previous folders should get a greyish highlight, then in the last column where I clicked something has normal highlight — that is the key column and selection."* The drawing already followed it, and that is now **proved rather than asserted**: `tst_ColumnsPane` clicks a folder, then a row in the column it opens, and reads the colours off the pills — the row last clicked is `Theme.accent`, the folder behind it `Theme.surface`, a row nobody chose has no pill at all; `columns_ops.py` leaves `tests/e2e/out/columns-highlight.png` of the real window showing the same thing. The operations were what did not follow the rule. Now `ColumnsPane.selectedUris()` answers with the row highlighted in the key column, and **`Shell.selectedUris()` asks the view before the pane** — so every destructive path, the IPC `selection` and `state` included, reads what is on screen. Nothing highlighted means nothing chosen, and `Del` does nothing rather than reaching for a selection left behind by another view. **Tests**: `tst_ColumnsPane` (the key column's row is what is chosen; a row in a deeper column is chosen where it lives) and a new flow **`columns_ops.py`** (8 checks: the highlight is what the window says is chosen, `Del` takes it and leaves the other view's choice alone, a deeper column's row is chosen while the pane stands elsewhere, and `Del` there takes that row) — which fails 5 of its 8 with the shell's one-line change reverted. `shell state` also reports the listing's `error` now, so a flow can tell an empty folder from a listing that failed — which is how the FTPS puzzle below was pinned down. **A regression of that change, found on review 2026-09-21 and fixed**: `selectedUris()` was switched to the columns without looking at the callers that pair it with `pane.selection.current` — the `project` action then judged `isDir` from the *pane's* row (a TypeError when the pane had none, the wrong folder when it had one), and `editSelected` likewise. `Shell.selectedRow()` asks the same place as `selectedUris()` now, and `columns_ops.py` opens project mode on a highlighted folder while a file is the pane's selection (it fails on the old line). **Both done in phase 7, 2026-09-21**: the `Menu` key and `F2` in Columns act on the key column's row.

**Three in the daemon, found by an e2e failure and fixed (2026-09-20).** The symptom: after `remote_transfers` had run, the shell listed an FTPS folder that plainly had a file in it as **empty, with no error** — `count 0, done true, error ""`. The chain, once pulled:

- **`locations::remove` never told the plugin.** It dropped the daemon's record of the browse session (`sessions().remove(name\0browse)`) and left the plugin holding the connection — where `Ftps::connect` (`plugins/kiki-plugin-ftps/src/main.rs:393`) hands back a cached session **keyed by location name and role, without ever looking at the config**. So the next location to take that name inherited a connection to the server that was removed, and FTP answers `LIST` of a path that is not there with an empty listing and a 226, not an error. For a user: remove a location, add another under the same name, and you are browsing the old machine with nothing to say so. `remove` now calls `disconnect(name)`, which drops every session it has, job roles included, and tells the plugin each time. The plugin's blind cache is left as it is — **the defence in depth was added in phase 6, in both plugins**: compare the config before handing a session back.
- **A removed location's password stayed in the keyring.** `remove` asked `secret_keys(name)` for the fields to clear *after* writing the file without that location — so `find` had nothing to answer with, the list came back empty and `keyring::clear` was never called. It reads the fields first now.
- **`disconnect` could hang the daemon outright.** It held the `sessions()` mutex across the plugin round trip, and a plugin that logs while it answers takes that same mutex (`jobs_using`, for the job log) — a deadlock, and after it the daemon answers nothing, ever. Found by making `remove` call it: `RemoveLocation never answered`. This is not only that path: **the sidebar's own "Disconnect" runs it** (`server.rs:459`), so a live location was one menu click from a frozen daemon. Sessions are taken out of the map first and told second, which is what `JobSessions::drop` already did.

**Tests**: the pair `remote_transfers` then `drag_between_panes` is the regression — it fails without the first fix and hangs without the third. A Rust test of its own was written in **phase 6** (`location_lifecycle.rs`) (`remove` clears secrets and disconnects; `disconnect` does not hold the lock).

**Also seen, also not chased — the gallery blows small pictures up.** Its two frames are `cache: false` with `sourceSize` at twice the stage and `PreserveAspectFit`, and under that combination Qt decodes *to* the source size rather than capping at it: a 120 × 80 PNG comes back 1600 px wide (a plain `Image` of the same file keeps 120; seen under `qmltestrunner`, Qt 6.11.2). `fit` then shrinks the 1600 to the stage, so a small picture is shown stage-sized, not at its own size — the frame's comment says "never blown up" — and `1` ("actual size") shows the upscaled decode, not the file's pixels. Wrong on screen, so by this plan's rule it is fixed; not in phase 5. Check by hand first: a 64 px icon in Gallery.

**Tests**: `tst_DragDrop` / `tst_DropAction` grow the ask, refused targets, modifiers read at drop time, focus to the receiver, and the gallery and columns sources carrying a selection. `drag_between_panes.py` as above. What stays manual is the physical press–move–release, and it goes on the phase 10 checklist.

**Sway spike** (½ day, time-boxed): `run_in_sway()` in `run.sh` (`output HEADLESS-1`, `exec` the script, `swaymsg exit`), to find out whether sway's headless backend delivers virtual-pointer events to Quickshell. If yes, `pointer_ops` (14 click checks) stops skipping and `sway` joins CI. If no, the skip stands and `11-testing.md` records why. Either way the answer is written into plan 11.

### Phase 6 — Side by Side and mirror, driven (3½ days) — **done 2026-09-21, but for what a hand is owed**

Built by three agents working side by side in one tree, each in its own files, and checked afterwards: every diff read, every new flow and suite run again, and a fix taken out to see its test fail.

- **By hand first** (½ day + fixes), plan 29 A's three lists — plans 07, 08 and 24 as amended by 29 J — on two local folders, then a real remote. **The mirror list was driven by the hooks below instead of by hand, 2026-09-21, and what a hand is still owed is short**: the physical clicks (cage maps no pointer), "Save report…" writing the file through `execDetached`, and how the screens look under a long run — they join the phase 10 list. Everything else on plan 08's list passes and is now a test, including cancel mid-run against `sshd` at 192 MB, after which the pane still lists the folder the run was writing into. One line of plan 29 A was wrong on screen when first driven: the Done summary.
- **IPC hooks for `MirrorWorkspace` — done 2026-09-21.** `shell mirror <action>`: `open` / `download`, `set <option> <value>`, `preflight`, `tab`, `check <row> on|off`, `report`, `run`, `confirm yes|no`, `cancel`, `back`, `close` — one string, space-separated, because IPC hands a function strings. Each calls the function the click calls: the Configure controls were factored into `setDirection`, `cycleDetector`, `toggleDeletes`, `toggleFilters`, `toggleWindow`, `back`, `answerLargeDelete`, `fetchReport`, and the buttons call those too, so the hook and the hand cannot drift apart. Every call answers the whole workspace — screen, options, plan counts, the plan's rows, the question if it is up, the Done summary, the report — so a flow never guesses.
- **`mirror_local.py` — done 2026-09-21, 55 checks** (in the default run): Configure's defaults, scan → plan → run → done, the destination tree entry for entry, the report's header, summary and per-action lines, a second run with nothing to do, one file edited planning exactly one changed copy, a row unchecked and then not run, deletes off leaving extras alone and deletes on taking them, the audit log's line per delete and its run summary, and every guard refusing: the blast radius (asked, declined, nothing touched; confirmed, five deleted), an empty master, a filtered name never being an extra, a folder mirrored into itself, and no run without a plan.
- **`mirror_sftp.py` — done 2026-09-21, 21 checks**, on the phase 4 fixture: an upload of times spread over twelve years that a second run finds nothing to do; a run cancelled at 192 MB that ends cancelled, says so, leaves no half file under its own name and no part file, and leaves the browser listing that folder; and one FTPS run where `vsftpd` is installed — the same four screens, and the report showing the size-only detector that plugin asks for. Skips by name without `sshd`; the FTPS half skips without `vsftpd`.
- **Six found while driving it, all fixed.** The Review footer joined its two copy counts instead of adding them ("31 copy" for 3 new and 1 changed). The Done summary said only "Mirror complete", and a cancelled run showed the advice given before one starts — it is plan 08's line now (`Mirror complete · 12 copied · 1 deleted · 2.0 KB`, `Mirror cancelled`, `Mirror failed: …`). Re-opening the workspace came back on the last run's table over a closed listing. A scan or run that finished before its own `Submit` answered was never noticed, and the workspace waited for ever (it reads the job out of `Jobs.list` now; the race could not be made to fire on demand, so this one has no test). **A folder could be mirrored into something inside it** — the tree copied into itself, and with deletes on the copy then taken for extras — refused before the scan now, **in the workspace and in the daemon both** (`mirror::scan::overlap`, `a_folder_is_not_mirrored_into_itself`), so a client that skips the form is refused too. And the engine's audit log ignored `KIKI_STATE_DIR`, so every e2e run appended its mirrors to the audit log in the developer's own home (145 lines of them). `tests/qml/tst_MirrorWorkspace.qml` (19) pins the two footers, the refusals, the options and what closing forgets — plan 29 A's "no QML test of `MirrorWorkspace`" is closed. Checked: with the overlap guard taken out `mirror_local` fails 4 of its 55.
- **The three mirror controls plan 08 drew and nobody had built — decided by the owner and built, 2026-09-21.**
  - **Concurrency is five**, hard-coded in the workspace and as the daemon's default, with no selector; the label says what is used. *What five means*: `execute` resolves each side once and every worker shares the job's one session, and a plugin process carries one binary stream at a time (FTPS serialises everything behind its session mutex) — so against a server the five overlap the mkdirs, deletes and set-times around one transfer, and it is local-to-local runs that go five files at once. A whole `mirror_sftp` run held at most two connections to `sshd` and one to `vsftpd`.
  - **The manual clock offset**, on the Configure row it already had, in the style of the row above it: a check "Set manually", a whole-hours box (−24…+24, out of range refused, dimmed while automatic) and a line saying which way the hours go — *"The destination's clock is 2 hours behind the source"* — because the offset is `master.mtime − replica.mtime` and a sign the wrong way about re-copies a whole tree or skips one. `shell mirror set offset auto|<hours>`. The report now names the direction too (`7200000 ms (manual, subtracted from master mtime)`; it printed the bare word "manual"). Tests in Rust, `tst_MirrorWorkspace` and `mirror_local.py`; the engine's sign flipped fails three of each. *Still nowhere on screen*: the offset the scan measured — it is in the report only.
  - **Edit rules…** (owner: *"really just a filter of what not to transfer — hidden files, and whatever the user specifies"*; a rules dialog, chosen from three shapes). `qml/kiki/ui/MirrorRulesDialog.qml`: one row per rule — the kind cycling is → contains → starts with → ends with, the name, an x — with + Add rule, Restore defaults, Cancel, Done; behind an "Edit rules…" button beside the filters check. Daemon: `MirrorFilters` / `SetMirrorFilters` (the old `Filters`/`SetFilters` had no caller and no validation — an unknown kind was coerced, a bad value dropped, silently), checked in full before anything is written; "the user deleted every rule" is `defaults = false`, since an empty file means the defaults. Saving over a plan on Review goes back to Configure. **The defaults are eight names, each an `is`** — `.git`, `.DS_Store`, `.env`, `.idea`, `.vscode`, `Thumbs.db`, `node_modules`, `__pycache__` — decided by the owner on the day, in place of the `startsWith "."` they were first built as. That one pattern silently kept `.htaccess`, `.htpasswd`, `.user.ini`, `.nojekyll` and everything under `.well-known/` (Let's Encrypt's challenge among it) from ever being uploaded by a website mirror; nothing on the server was deleted, but the only sign of it was "N filtered out". A dotted name is mirrored like any other now unless a rule names it, and a user who wants every hidden file skipped adds `starts with .` in the dialog — where the dimmed "hidden files" hint still marks it. **One list, and it is the daemon's**: `MirrorFilters` and `SetMirrorFilters` both answer with `defaultRules` beside `rules`, and "Restore defaults" previews what the reply gave it, so the dialog carries no copy of the defaults to drift. Found on the way: a plugin's recursive `Scan` counted every descendant of a skipped folder ("6 filtered out" over SFTP, "1" locally). (A Review tab listing what was skipped was offered and declined — owner, 2026-09-21.) `tst_MirrorRulesDialog` (20), `tst_MirrorWorkspace` (36), `mirror_local.py` 61 → 83 (re-aimed at a fixture carrying `.htaccess`, `.well-known/acme-challenge/token`, `.env` and `.git/HEAD`); a picture at `tests/e2e/out/mirror-rules.png`. Checked: with `startsWith "."` put back, 3 Rust tests and 6 of `mirror_local`'s checks fail on `.htaccess`.
  - Also: an empty sub-plan runs an empty job and says "Mirror complete · 0 copied" rather than closing, and the report prints `Copy/New` where plan 08's sample prints `COPY/NEW` — phase 9.
- **What plan 29 A called `split`, finished inside `side_by_side.py` — done 2026-09-21. 41 checks → 75, and nothing in the product had to change.** The transfers are driven by the keys: `Ctrl+C` / `Tab` / `Ctrl+V` across the panes, and `F6` for the move; each is followed by `Ctrl+Z` and a byte-for-byte comparison of both folders against how they started. Between the transfer and the `Ctrl+Z` the flow waits for the **job** to end, not for the file to appear: a copy reaches the journal when it finishes. **Selections back after collapse and expand, both directions** — collapsing from the right swaps the two `Pane` objects, collapsing from the left destroys the right pane's view; both keep each pane's folder and selection, and the pane that stays is remembered as Icon view so its view is rebuilt on the way out. **The three that want a server** run on the `sshd` fixture (skipped by name without it): `openLocation` puts its local folder left, its server right, the focus right; `Ctrl+4` puts **home** on the right when no location has been opened and **that location** once one has; a remote URI **typed into the breadcrumb** pairs with its location's local folder, from the awkward state — collapsed from the right. **Proved to bite**: `leaveMirror` made to copy the URI across instead of swapping the panes fails 11; `transfer` inverted fails exactly one.
  - **Undo of a copy to a server — built 2026-09-21 (owner: "build undo for copy to a server only").** A copy whose destination is a server journals `deleteCopies`: every file it created, with the size and time the **server** reported as it landed, and every folder it created — never the folder it landed in, never a skipped item. The undo deletes only what the server still says is that, by the check the mirror makes about that server (`pick_detector`: sftp size and time, ftps size alone), leaves what has changed or gone and names it, and removes a folder only if it is empty afterwards. Both toasts say a remote delete is permanent: *"Copy site to www — Undo deletes it from homelab, permanently"*, *"Undid copy — 3 items deleted from homelab (permanently), 1 item left because it had changed"*. A copy cancelled or failed half way journals the part that arrived; it survives a daemon restart; redo copies again. `kikid/tests/undo_remote.rs` and 21 checks in `remote_transfers.py` against `sshd` and `vsftpd` (sftp → ftps deletes on ftps only). **A move that touches a server is still not undoable, deliberately.** Found beside it and fixed: a job said "done" *before* its inverse reached the journal, so a client quick enough to answer with `Undo` took back the operation before — the end is announced after the journal has it now (`jobs.rs`). Known and left: a `hidden` job that fails is seen nowhere.
  - Plan 07's pane-header badge from the plugin's `display_name` is stale (29 J dropped the chip). `enterMirror`'s `lastLocation` fallback is dead code, harmless. `side_by_side` must stay the first flow to touch the layout: `right.uri` is sticky for a window's life, so "home when there is no location" can only be asked once.
- **Small tests — done 2026-09-21.** **`pick_detector` per plugin and direction** (`kikid/tests/pick_detector.rs`): the real plugin binaries under the names they install as — `smb` and `dav` being gio twice — spawned for a real `Describe`, and both halves asserted: what each declares (sftp sizeMtime both ways, **ftps sizeOnly up and sizeMtime down**, gio sizeMtime both ways) and what `pick_detector` then picks; the stub declares `digest` one way and an unknown word the other; local-only, a scheme with no plugin, and an explicit choice beating every plugin. mtp, ptp and afc are out of it by construction (`plugin::ships()` refuses them). **A plugin killed mid-`Read`** (`plugin_killed.rs`): a real copy job reads through the stub, the process group is SIGKILLed while bytes are landing; the job **fails** naming the file and saying `plugin exited`, no short file and no part file are left, and the next request gets a **new** process that copies the same file whole. **Plugin-supplied `Meta.hidden`** (`plugin_hidden.rs`): decided in the daemon (`rebuild_view`), tested there. **The view-by-contents default (D18)** (`tests/qml/tst_PaneSmartView.qml`, nine): a picture folder with no memory opens in **Gallery**, by name or by contents (sixty per cent, twelve rows at least, the first two hundred looked at); memory wins; the pane's own guess is not written to view memory; side by side makes no guess. Plan 24 is amended in phase 9.
- **FTPS and SFTP: a cached session is only the server it was made for — done 2026-09-21.** Both plugins kept a session by location name and role and handed it back without looking at the config — **SFTP had exactly the same hole as FTPS**. Each now compares a pure `identity` (host, the port it will dial, username, TLS mode or auth tab and keys, pinned fingerprint, `insecure`, the secret it will send), joined on NUL; a session that does not match is disconnected and made again. A unit test per plugin over every field, and an end-to-end test per plugin against **two mock servers**; both fail against the old cache.
- **`locations::remove` and `disconnect`, in Rust — done 2026-09-21** (promised to phase 7). `kikid/tests/location_lifecycle.rs` runs `remove` under a watchdog, so the deadlock is a failing test: the location goes, both its sessions are told, the keyring is cleared; the sidebar's Disconnect is in the same test. Restoring either old behaviour fails it.
- **`make test` had been failing with every check passed — found and fixed 2026-09-21.** `test-e2e` ended in `Error 124`: cage stays up while anything is drawing in it, and the project-mode check added to `columns_ops.py` that morning starts two `foot` terminals, detached, which outlive the shell — so the run sat until the compositor's deadline (600 s, and 1800 s when it was raised to look). `run.sh` now ends whatever was started inside the run before the script leaves, known by the run's own `XDG_RUNTIME_DIR` in its environment, which nothing outside the run has. The whole e2e run: **549 passed, 0 failed, 1 skipped, exit 0, 57 s**.
- **Seen, not reachable today**: `listing/scan.rs`'s in-place patch decides a new row's visibility by the dot-name rule alone and never looks at `Meta.hidden` — it runs only from inotify and a remote folder is not watchable, so it becomes a bug the day a plugin gets a watcher.

### Phase 7 — test debt that guards shipped behaviour (3½ days) — **done 2026-09-21**

Built by three agents in one tree, as phase 6 was, and checked the same way: diffs read, suites run again, a fix taken out to see its test fail (the trash paste guard: `tst_TrashView` 1 of 10; the gallery's fill mode put back: `tst_GalleryPane` 4 of 24).

- **Watch — done** (`kikid/tests/watch.rs`, its own process). Touch, create and delete, fifteen times each, timed from the `write` to the event: the fastest of each run inside plan 01's **100 ms**, every one arriving (the fastest, because the suite runs beside other builds, and a raised debounce or a missing watch moves *every* sample). A touch is a `Rows` of exactly that row; the others a one-op `Splice`. Two windows both told, each under its own `lid`. Eviction at 64 keeps `Meta`.
  - **Found and fixed: a folder you came back to was never watched again.** An evicted listing is marked stale; `listing::open`'s cache-hit path re-listed it and returned without a watch — so from the sixty-fifth folder of a session on, every folder revisited was listed once and then quietly stopped keeping up. Watched again on that path now (`listing/cache.rs`).
- **Undo round trips — done** (`kikid/tests/undo.rs`): rename (file and folder), mkdir, chmod (recursive and single), same-disk move, restore, compress (zip, tar.gz), extract — each **done, undone, redone, undone**, the **whole tree** compared each time (names, bytes, modes, mtimes). **Undo after a daemon restart**: a real `kikid` does two jobs, is SIGKILLed, and a second daemon on the same state directory undoes both in order. Bites: `load_journal` returning nothing; a `chmod -R` inverse that skips directories; `movePairs` reversed.
- **JSON — done** (`crates/kiki-json/tests/property.rs`, seeded, no new dependency): `parse(print(v)) == v` over 2,000 documents; strings spelled wholly in `\uXXXX`, surrogate pairs included; 13,000 mangled inputs never panic. Pinned rather than hidden: a non-negative `Int` and a whole `Float` come back `Uint`, `-0.0` as `Int(0)`, NaN/∞ as `null`. **The protocol** (`kikid/tests/protocol.rs`): all **96 request types, scraped from `server.rs`'s own match arms**, through both framings and `parse_request`; the 20 events the daemon really sends; `ok`/`err` over every code; the framing's edges.
- **Share — done** (`kikid/tests/share_contract.rs`): the three shipping binaries' `Describe` pinned (LocalSend ships **off**, the other two on); `share.rs` end to end against a stub built only under the `stub` feature — a folder the plugin will not take is zipped, a remote file is fetched first, progress and Toast carried, scratch cleared however it ends.
- **Small — done**: `kikid/tests/local_ops.rs` (mkdir / rename / delete / set_mtime and what each refuses; **ten modes including setuid, setgid and sticky against `stat -c %a`**; the resolver — `~`, no `HOME`, an unknown scheme as a typed error at all three layers); heat-map compaction at 50,000 lines (`access.rs`); the `atime` sort (`listing/tests.rs`). The 1,000-folder cap was already covered (`config.rs::the_cap_drops_the_oldest`). The *symbolic* permission string is `Inspector.qml`'s, not the daemon's — not yet tested.
- **`tst_SettingsWindow` — done, 5**: the default view is a one-key `SetSettings` patch; a tree width outside 48–2000 is refused at the field; Reset all; the AI provider goes through `AiConfigure`.
- **Git badges in QML — done, 11** (`tst_GitBadges`). **The colours were hex literals and never followed the theme**; they are the palette's now (`deleted`/`conflicted` are `danger`). On the default theme one shade moves: untracked `#7f9e6a` → `#7e9c78`. `git_status.py` also checks the badge *on screen* going after an outside commit (7 → 9).
- **Open in — done, 10, and `Alt+Enter` had never worked**: the request named the tool `id`, which overwrote the request's own number; the daemon answered "missing id" to nobody. It is `tool` now on `OpenIn`, `OpenInTest`, `OpenInClose`. `tst_OpenIn` builds the **whole `Shell.qml`** — the first test that does, on `FloatingWindow`/`IpcHandler` stubs. **Plan 14 amended the same day** ("As built"): the ways in are Open AI here…, Open Terminal here…, Edit, project mode, `Alt+Enter`, and Open with ▸; a picker of tools is not in 0.1.0.
- **The trash view — done** (`tst_TrashView` 8, `trash_view.py` 6 → 16). Only the *menu* knew: `Super+V` pasted into `trash:///`, `Ctrl+Shift+N` made a folder there, a folder row inside the trash took drops. Refused in `Ops` and in `Pane.dropAction`. `shell dropOn … trash:///` (the view) is refused while `shell drop … trash:///` (the sidebar's Trash) still trashes. Also found: **"Restore to ome/t"** — `Format.display` cut two characters off a bare path; and **the trash view never followed the trash directory** — the listing cache is keyed by URI and the trash is the one listing whose URI is not its path, so no watch event ever reached it (`cache::at_path`). Its Rust test is `kikid/tests/trash_watch.rs`: open `trash:///`, trash a second file, empty it, and the view follows each; with the fallback taken out it fails on the first.
- **Cheat sheet — settled by removal (owner, 2026-09-21: *"remove the shortcuts overlay — does it not duplicate what's in the rebinding window?"*).** It did: its rows were a subset of `Keymap.qml`'s, and nothing but an IPC call could open it — the bar said `? keys`, `?` was bound to nothing, and `Ctrl+?` opens the rebinding window. `ShortcutsOverlay.qml`, its IPC and its test are gone; the bar's hint reads `^? keys`. `config.rs`'s third copy of the key table — stale, and **used by nothing** — went too, with the `Keymap` request (API-DAEMON and plan 20 to follow in phase 9).
- **Columns: the `Menu` key and `F2` — done.** Both ask the view before the pane (`Shell.contextItemsNow()`, also behind the `contextMenu` IPC). `F2` opens **the editor list view uses** on the key column's row, without leaving Columns: `ListRow`'s editor became `views/RenameEditor.qml` and both views show that one component. `tst_ColumnsPane` +4, `columns_ops.py` 19 → 27. **Then, the same day (owner): Rename in the row menu, and `Ctrl+Shift+N` that stays in Columns.** The row menu carries **Rename** — labelled, keyed (`F2`) and placed as list view's is, no new separator — acting on the row the menu belongs to, deeper columns included. **New folder** is made in the **key column's folder**, lands as that column's highlighted row and opens the inline editor with the name selected; Escape leaves it "New folder"; the view never changes. `Ops` still knows nothing about views: it asks `listingNeeded({ dest }, reply)` — beside `confirmNeeded` and `folderNeeded` — and the window answers with the column that folder is in, or nothing, where list view's old path runs unchanged. Found on the way: the FIRST column's background menu would have made its folder in the key column. `tst_ColumnsPane` +6, `tst_Ops` +2, `columns_ops.py` 27 → 39; the old route fails 11. **Where it lands (owner, 2026-09-21: "should land inside key highlighted folder")**: inside the folder highlighted in the key column — you clicked Projects, the new folder is one of Projects' — and in the key column's own folder when what is highlighted is a file, or nothing is (`ColumnsPane.newFolderUri()`, which opens the highlighted folder's column if it is not showing, since that is where the row and its editor will be). `tst_ColumnsPane` +2, `columns_ops.py` 40 → 44; with the old aim the flow fails two. *Left*: a rename leaves nothing highlighted and a new folder landing outside the loaded window keeps its name — both true of list view too; the `Menu` key opens at a fixed point in every view.
- **The gallery blowing small pictures up — fixed, and it was one property.** `fillMode: PreserveAspectFit` on the frames turned `sourceSize` from a ceiling into a size to scale **to** (120 × 80 → 1600 × 1067; a 3000 × 600 panorama → 5160 × 1032). Without it the cap caps. `gallery_perf` over a thousand photographs: **513 MB against 589 MB**, decode 11 ms against 14. *Not changed*: "actual size" on a picture larger than the cap still shows the capped decode.
- **"Leaving project mode left the window with no keyboard" — reported as a defect in phase 7, and corrected the same day: it was the test bench, not kiki.** Project mode starts an editor and an agent, each in a terminal of its own; under cage those two windows took the compositor's keyboard, so no key sent afterwards reached kiki — which on a desktop is simply the new terminal having the focus. `leaveProject` still calls `keys.forceActiveFocus()` (harmless, and what every dialog's `onClosed` does), but it was never the cure: **with the terminals out of the way the keyboard works after project mode with or without it** — checked both ways. What was really wrong was the harness: whether kiki or a terminal had the focus at a given moment depended on which started faster (the check failed once in a whole run, never alone), and the terminals killed the keyboard for every flow after. `run.sh` now writes an `open-in.toml` whose editor and agent are programs that start, draw nothing and end — the tools are there to be started, not used. `columns_ops` checks the keys are back **and that a real chord is heard** (`Ctrl+4`), and project mode need no longer be the last thing a flow does.
- **`make test` went green over failed e2e checks — found and fixed 2026-09-21.** cage does not hand back its client's exit status, so `run.sh` left as 0 whatever the driver said: a whole-suite run printed "653 passed, 1 failed" and `make` called it a pass. (The `Error 124` fixed in phase 6 had been hiding this: the run used to die at the deadline instead.) The driver's verdict is written down inside the compositor and read outside it; no verdict at all is a failure. Checked both ways with a flow that fails on purpose.
- **A flake in `remote_transfers`, found by the whole-suite run and fixed.** "The upload was still running when the folder listed" failed once in four whole runs and never alone: on a loaded machine the listing is slow enough, and loopback fast enough, for 1.5 GB to be over first. The pair is tried twice now. A listing that *waits* for the upload loses both times — it comes back only once the upload is done — so the second go forgives the machine, not the bug.
- **Redo — done**: `Ctrl+Shift+Z` in `ops_keyboard.py`, and `Ctrl+Z` after it.
- **The branch capsule — built 2026-09-21** (owner: *"why not just make that a branch capsule that has same colors as dot and shows branch too?"*). Plan 15 has it in full. `Theme.changed` came out of its first picture: matte-black's `yellow` is `#b91c1c`, so once badges followed the theme a modified project wore the colour of a conflicted one.
- **The developer's own audit log — emptied and the leak closed, 2026-09-21.** All 2,022 lines of `~/.local/state/kiki/audit.log` were tests' — not one real mirror. The e2e half was closed in phase 6; the Rust half was still writing seven lines per `cargo test` (`mirror::tests` twice, `plugin_contract.rs`), which now set `KIKI_STATE_DIR`. A whole `cargo test` leaves it at 0 lines.
- **LocalSend and WebDAV removed from 0.1.0; no SMB1 — owner, 2026-09-21.** LocalSend went the way Messages and AirDrop did (plan 18): plugin, workspace member, PKGBUILD, tests; `off_by_default` went with it, having no other user and no documentation. WebDAV: `LOCATION_KINDS` is `ftps, sftp, smb`, nothing installs `kiki-plugin-dav`, the gio plugin keeps its `dav` arms beside its `afp` ones, and a saved `dav` location stays listed and says *"WebDAV is not in this version"* (`location_lifecycle.rs`). **SMB2 or later only** (plan 25): kiki passes gvfsd no protocol option, so libsmbclient's minimum stands; a server offering only NT1 is told *"This server only offers SMB1, which kiki does not support"* — matched on Samba's message text, **read from its source, not observed: confirm in phase 8 against `server max protocol = NT1`**. Plan 25's Encryption field was never built and is struck. Found beside it: `share_contract.rs` could test stale plugin binaries (`cargo test` does not build another package's binary) — it now says so instead of arguing.
- **Plan versus build, for phase 9**: plan 15's "branch name under its name" is replaced by the **branch capsule** — built 2026-09-21, written up in plan 15; plan 20's Keys / Locations / Open in / Devices / Jarvis settings pages do not exist; `IndexProgress` and `SharePluginsChanged` are documented and emitted nowhere.

### Seen while running the app (owner, 2026-09-21, evening) — each built or fixed the same day

- **The columns row menu had fallen behind list view's** — no Paste, no New folder, no "Extract to…", none of the ways of sending (*"columns view does not have the send with tail and mail items"*). Row for row the same list now; `tst_ColumnsPane` fails the day one gains a row alone, and "Extract to…" on a deeper row is about that row and starts its chooser where the archive is.
- **"Connecting to homelab…" in a remote pane** (*"so user knows something is happening"*): `ui/PaneConnecting.qml`, a ring and a bead over the pane while a server's folder has been asked for and nothing has come — after 300 ms, so a quick server flashes nothing; an error hands over to `PaneError`. `tst_PaneConnecting`.
- **List columns are dragged to size** (*"need ability to drag resize list view columns"*). The edge a value column **begins** at is its grip — the value columns are laid out from the right with Name taking the rest, so that is the only edge that can follow the pointer; 8 px, the system's resize cursor, nothing drawn; a double click resets. Global to list view in `settings.toml [view.listColumnWidths]`, written once on release. A narrowing pane squeezes the columns towards their header-sized minimums before it drops any. `shell listColumn <role> <px>|reset`. `tst_ListPane` +9, `view_state.py` +9. **Found beside it: any map-shaped setting made `settings.toml` unreadable** — `toml::write` put a table inside a table out as one line of JSON, which `toml::parse` refuses, so the file read as empty (every setting back to default) and was never written again. Live in the build through `[mirror] last`, written after every mirror run: the owner's first real mirror would have cost him his settings. Nested tables go out as `[mirror.last]` now. And every settings write rebuilt every list cell (`columns` came straight off `Settings.view.columns`); keyed off the role list now.
- **Cancelling the mirror compare** (*"I hit Back, then went back into the screen and it was still showing 'Comparing…' text"*). The way out of Preflight says **Cancel** now, and Escape does the same there. `back()` never cleared `status`, and that text was drawn in the danger colour whatever it said; a Cancel pressed before the `Submit` reply was dropped and the compare ran on unseen; the id was kept after Back, so a `done` in flight pulled the workspace through to Review. **And the compare itself did not stop**: the daemon polled the cancellation token only when a frame failed to arrive, so a plugin streaming fast enough was never told — a stub scan of 50,000 entries was walked to the end and the job ended *done*; it stops at 600 now, cancelled 5 ms after the ask. Connecting was not cancellable at all (120 s); 732 ms now. The digest path read whole files into memory; MD5 is incremental and gives up inside a file. Preflight reads "Comparing ~/site…  12,400 items so far". `kikid/tests/mirror_cancel.rs`, `tst_MirrorWorkspace` +8, `mirror_sftp.py` 21 → 27 on 20,000 files a side. Left: over SFTP the count comes in bursts (`fast_scan` holds entries until `find` finishes, on purpose).
- **Settings → Share never drew a plugin's form** — the delegate asked for a `fieldData` no Repeater supplies; sixteen "Cannot create delegate" lines in one session's log. Drawn now; `tst_SettingsWindow` checks a secret field travels as a secret. Three other startup warnings (gallery strip delegates outliving their view, two `undefined`-to-bool) fixed at the source — plan 29 F's clean log.
- **A window that was closed never came back — and what closing means (owner: *"if kiki is not running — stuff should not continue in background"*).** Quickshell only hides a closed `FloatingWindow` and keeps the shell up behind it, and a hidden one cannot be shown again (`visible = true` does not map it — tried live): the next `kiki` found a running shell, asked it to come to the front, and nothing came. **Closing the window quits kiki** now (`onVisibleChanged` → `Qt.quit()` once it has been shown); the daemon stays, as it must — it is a socket-activated service that also answers the desktop's file chooser and `FileManager1` — but idle: **jobs the user started do not run on behind a window that is gone.** **Done 2026-09-21, both halves.** *The window asks*: Quickshell's `FloatingWindow` has only `closed`, which is after the fact, but the real window behind it (`contentItem.Window.window`) has Qt's `closing`, and that can be refused — proved in a headless cage, a refused close leaves the window up and the next one goes through. A close over running jobs is held and the existing `ConfirmDialog` asks *"Quit kiki? “site” is still running and will be stopped. What has already finished stays where it is."* — Quit cancels every live job and quits, Cancel keeps both; machinery alone (a hidden compare) is not worth a question. `tst_CloseWithJobs`, 7. *The daemon stops what a window left behind* — a kill, a crash: a connection that says `Hello client:"kiki"` is a window, the jobs it starts (`Submit`, `Share`, `Undo`, `Redo`) are marked its own, and when the last window has been gone for 4 s (`KIKI_SHELL_GRACE_MS`) those still live are cancelled, with "kiki was closed: stopping" in each one's log. A window back inside the grace keeps them; a script's, the portal's and a test's jobs are never touched. `kikid/tests/shell_gone.rs` — the cancel taken out, it fails.
- **Tailscale, "shared 2 files and neither came through"**: both jobs ended done and `tailscale file cp` had succeeded to a valid online target — the receiving side. But the job log never said **which** peer and Tailscale's own refusal text was thrown away; the toast and the log now say "sent — 1 file to davids-macbook-pro, waiting in its Tailscale", and a refusal carries Tailscale's words.
- **"Open with" takes a selection** (*"open with also needs to support multiple selection"*). Several files chosen got a dead row, "Select one file". `OpenWith` takes `uris` now and answers with what opens all of them — text and markdown share Code and Neovim, a text file and a picture share nothing and the row says "No application opens all of these" — and the app chosen is handed the lot. **And `Launch` had never opened more than the first file for an app whose `Exec` takes one** (`%f`, `%u`): it is run once for each, as the Desktop Entry spec has it. `desktop.rs` +2 tests, `tst_OpenWith` 4.
- **The gallery's filmstrip showed the wrong file's picture** (*"gallery view is showing text file thumbnail for images"*). The strip recycles its tiles; a tile held its row as a value from the first `rowsUpdated` on (the assignment replaced the binding), and nothing read it again when the tile was handed another place — so a recycled tile showed the file of the place it had before, which in a folder of mixed kinds is a text file's icon where a picture is. List, Icon and Columns all re-read on an index change; the strip alone did not. It reads its row again on a new index, on reuse, on rows updated and on a reset, and a tile with no row yet draws nothing rather than a guessed "file". The picture was never by position: `thumb` is the daemon's path for that file and travels with the row. `tst_GalleryPane` +1 — 400 mixed rows scrolled both ways; without the re-read it fails with "238 shows n97.txt wants p166.jpg".
- **The first click on the info panel's play button did nothing** (*"first click on info panel play button for video - seems to be missed"*). The click builds the player; the player asked for `play()` the moment it existed, and the panel's Loader hands it the file only after that — a `MediaPlayer` with nothing in it forgets the request. It plays when its source arrives now. `tst_VideoPreview` 2, against a real three-second clip (`tests/qml/fixtures/clip.mp4`); the old code fails it with "never started".
- **Icon view has a lasso** (*"icon view needs to support lasso"*). A press between the icons and a drag draws a band; what it touches is selected, live, and Ctrl or Shift adds to what was selected before — touching one of those again takes it out, as Ctrl+click does. Worked out from the grid's geometry rather than from the tiles on screen, so a band dragged past the top or bottom — the view follows, faster the further out — holds rows that have no tile any more. Two things made room for it: **a tile answers for its picture and its name, not its whole cell** (the rest of the cell is where a band starts, and a press there no longer picks the file up), and **a mouse drag no longer flicks the grid** (`interactive: false`; the wheel and two fingers scroll). A click on empty space lets go of the selection. `Selection.setMany`; `tst_IconPane` +6.
- **Columns: the wheel walks the columns, and a column drags to a width** (*"use scroll wheel to move view back to first column … and even resize columns themselves"*). Shift and the wheel anywhere, and the plain wheel over a column with nothing of its own to scroll (`NaturalScroll.whenItFits`) — a mouse has no sideways wheel, and the keys were the only way back. The line between two columns is its grip: 7 px, the system's resize cursor, nothing drawn, a double click lets the width go. Widths belong to the column's PLACE ("c0", "c1"), not its folder, as in Finder; the columns not dragged share what is left; `settings.toml [view.columnsWidths]`, written once on release. `tst_ColumnsPane` +5. **Two found on the way**: the daemon merged map settings and so could never take an entry OUT of one — a list column reset with a double click (built this morning) was back at the next start; `null` forgets a key now (`Settings.forget`, `config.rs` test grown). And the shell told a list pane by its having `setColumnWidth`, which columns now has too — 19 `columns_ops` checks failed until it asked for `columnWidths` instead.
- **A click outside the path field is Escape** (*"clicking outside should do same as esc does"*). The field went when it lost the keyboard focus, and a click on the files takes focus from nothing. A window-wide catcher while the field is up closes it and lets the press through, so the row clicked is still clicked. `tst_BreadcrumbEdit` 4.
- **The mirror plan never re-laid itself out on a reset**: `parent.forceLayout()` inside a `Connections`, which has no `parent` — the name found the Loader, "forceLayout is not a function" 14 times an e2e run. By id now; the run's shell log has no TypeError left.
- **"Move to Trash on several items takes forever" — not reproduced, open.** The daemon's own log has the owner's job: 3 files, 1 ms. Timed end to end in cage, menu to view updated: 0.10 s in list, icon and gallery. Whatever was slow was before the job or not the trash at all; needs what was on screen while it waited.
- **Side by side, the keys scrolled the wrong pane** (*"on the remote side, I can't scroll to top/bottom items"*). Nothing to do with servers: `moveSelection`, `selectAt`, `rowStep` and `pageStep` all reached for `viewLoader.item` — the LEFT view — whichever pane had the focus, so on the right the selection walked out of sight and the list never followed. They use the focused pane's view now (`win.activeView`). And the right-hand view is built for the left pane and handed its own a moment later, after its height is set, so its listing was never told what was on screen (`viewport [0, 60]`): a view re-pointed at a pane tells that pane too. `side_by_side.py` +8, sixty rows a side, End and Home on each.
- **"Save report…" was unclickable** — and "Mirror" was off the window altogether. The Review footer was one row with a spacer of `width − 700`: right at one window width, and at the owner's (1176 px, the rail open, the summary long) the last buttons hung past the edge. Two rows now, one held to each edge, the summary eliding between them. `tst_MirrorWorkspace` +3 (640, 1132 and 1900 px; the buttons pushed off, all three fail). **And once it could be clicked it did nothing anyone could see** (owner: *"I hit it and it does nothing … should open save dialog"*): it wrote `~/kiki-mirror-report.txt` through a shell `printf`, silently. Now it opens kiki's own chooser in save mode with a name offered (`kiki-mirror-<host-path>.txt`), the daemon writes the file (`MirrorReport { job, saveTo }`) and a toast says where it went. IPC `mirror saveReport` / `mirror save <uri>`; `mirror_local.py` +3. **The header's paths wrap** (*"paths on preflight screen should wrap"*): each inside its own column, which is at most 340 px and narrows with the window, and the header grows to hold them; the screen below gives up the room. `tst_MirrorWorkspace` +1. **Buttons give something back now**: `Button` lights under the pointer and darkens with the accent on its edge for the length of a press — a button that gives nothing back looks broken.
- **A new icon for Mirror** (*"we need another icon for the mirror feature too"*): two arrows round a circle — a sync — in place of the arrow-each-way-across-a-dotted-line, which read as "swap" or "split". *Then* (owner: "mirror icon also needs a tweak") two stacked arrows, one each way: a transfer between two sides, which is what it is. Drawn in `gen.py` and `icons.js` together, as the set is.
- **Disconnect, beside Mirror** (*"a disconnect button — which disconnects the remote site"*). Side by side with a server in a pane, a broken chain — the unlink glyph everyone has seen — next to the Mirror button (it was a plug pulled from its socket, which the owner found unclear at 20 px): "Disconnect from ghs". It sends `Disconnect` for that location, moves any pane standing on the server to the location's local folder (or home — a pane left on a server that has gone can only say so), and, side by side being that server and its folder, goes back to one pane. The sidebar's menu item stays. `tst_ToolbarDisconnect`.
- **Side by side is a server and the folder beside it — decided 2026-09-21** (*"when you connect to a remote location it should activate side by side. The side by side button should be moved to where mirror/disconnect are. If you unclick side by side it goes to show the remote location only; turning it back on you see both. Side by side should not be toggleable/visible unless a remote location is open."*). Opening a location already paired it (plan 24); what changed: the button and `Ctrl+4` are offered **only while a server is open in a pane, or while the layout is on** (so there is always a way back); off, the one pane is **the server's**, whichever had the focus; on again from one pane on a server, the server goes right and its location's local folder — or home — left, as a location opened from the sidebar is laid out; **with no server open the key does nothing**. The button stands left of Mirror while on and where the path ends while off (the path gives it the room) — and is lit, box and all, while on: a first cut made it flat there, and the owner missed the highlight. `shell sideBySide toggle` may still split two local folders, which the flows use. **Seen at once by the owner**: off, a double click on a folder in the server brought it back — the pairing fired on every navigation to a server URI. It fires on *arriving* at a server (the host changed), not on a step inside one; and it survives the pane swap that leaving side by side does (`_leftHost`, reset `onLeftChanged`). `tst_ToolbarDisconnect` +1. **Plan 24 amended in phase 9**: "Ctrl+4 on `~/Projects` opens the last location on the right; with none, home" is struck. `tst_SideBySideChrome` rewritten, `tst_ToolbarDisconnect` +3, `side_by_side.py` and `columns_ops.py` reworked (the latter's "a key is heard" check uses the favorites panel now).
- **Columns view: the path follows the drilling, and a pill brings the columns back** (*"as we burrow into folders the file path above should update to the highlighted location"*; *"clicking a pill in the path area should bring column view back to that location"*). The pane stays on the first column's folder however deep the columns go — drilling opens columns, it does not navigate — so the path read `folderA` with C open under it. The path over the view (the title bar's, and each pane header's side by side) now shows the deepest open folder (`ColumnsPane.shownUri`); a pill that is one of the columns takes the columns back to it — it becomes the last column, nothing in it chosen, the trail keeping its grey — rather than opening the pane afresh there (`backTo`); a pill above the first column opens as before. `tst_ColumnsPane` +4, one through the real shell's title bar.
- **The chooser was cut off in a short window** (*"the save dialog opens in window and is cut off"*): `PortalDialog` was 860 × 560 whatever the window, and in one shorter than that its buttons were below the edge. It is that big at most and the window less a margin otherwise, with the Cancel/Save pair held to its right edge. `tst_PortalPick` +3 (1200 × 760, 1176 × 590, 700 × 420; the old box fails the small one).
- **`.gitignore` joins the mirror's default rules** (owner) — nine names now; the rules dialog's list fits nine before scrolling; every list of the defaults (the daemon's test, the dialog's, `mirror_local.py`) says so.
- **The sidebar's Disconnect did only half** (*"looks like it disconnects, but does not move user to a local folder"*): it sent the request and left the pane on a server that had gone. It goes through the same `disconnectLocation` as the toolbar's button — pane to the location's local folder or home, one pane again. `side_by_side.py` drives it against a real sshd.
- **A connected location wears a green dot** (owner): `Locations` answers `connected` per location — a live browse session — and the daemon sends `LocationsChanged` when one comes or goes, so the dot follows a connect and a Disconnect. `SidebarItem.dot`; `tst_SidebarItem` +1; the flow checks the dot on and off.
- **Configure fits a short window** (*"plan box is being pushed down into buttons … have plan text font get smaller … with a min of 12"*): the five option rows have fixed heights; short of room the gaps close first, then the PLAN box takes what is left and its type shrinks to fit (`fontSizeMode`, StyledText — RichText cannot shrink), to no smaller than 12 px; it never runs under the footer. `tst_MirrorWorkspace` +3 (760, 590 and 480 px tall). Also found: the Review list's `Connections` named the Running list's id, in another Component's scope — its own now.
- **A flaky Rust test, fixed**: `a_rescan_keeps_the_thumbnails_it_had` read a file's mtime before the meta had arrived (0 half the time), so the thumbnail it planted was "for another version" by the rescan. It waits for the meta.
- **Still leaking into the developer's home**: a Rust undo test wrote a failed job into the real `~/.local/state/kiki/failed-jobs.log` (17:00) — `joblog::keep_failure` after a test had already taken `KIKI_STATE_DIR` away. **Closed 2026-09-21**: all 43 entries in the real log were two jobs of `kikid/tests/undo.rs`, whose last loop — `while jobs::undo(..).is_ok() {}` — started undo jobs and did not wait for them, so they ran after the sandbox's variables had gone: against the real state directory, and the real trash. Each is waited for now. The whole Rust suite run afterwards leaves the file's mtime where it was.

### Phase 8 — real servers, real devices (1–2 days, mostly waiting on hardware)

One session each, against plan 29 B's list — list, copy out, copy in, rename, delete, mkdir, mtime kept, mirror second run empty, wrong password, disconnect while open:

- **SMB share** through gio — **done 2026-09-21, and the held day was needed.** A throwaway `smbd` and the real GVfs stack are a fixture now (`tests/e2e/servers.py::start_smb`: `smbd -F -s <conf>` as the launching user with its own passdb made by `pdbedit` — and *not* `--no-process-group`, which has `smbd` signal the test runner's process group on its way out) and two suites: `plugins/kiki-plugin-gio/tests/real_smb.rs` and `tests/e2e/flows/smb.py` (41 checks). The daemon alone runs under `dbus-run-session` with a `gvfsd --no-fuse` of its own, on the run's private `XDG_RUNTIME_DIR` — gvfsd-smb asks for the keyring before it asks the server anything, and from the ambient environment that is the developer's real one. **Seven defects, all fixed with tests**: a wrong password looped the mount prompt for ever (the pane waited out two minutes while the plugin went on offering the password to the server, several times a second); sessions were keyed by location alone, so the first transfer's `Disconnect` took the browser's session and every mkdir, rename and delete after it failed "not connected"; the whole `GIOErrorEnum` mount mapping was dead code (gvfsd-smb answers `G_IO_ERROR_FAILED` and an errno sentence) and is worked out structurally now — credentials from the second `ask-password`, the share by mounting the server's own root; the SMB1 match was written from Samba's source and the real words are *"Failed to mount Windows share: Software caused connection abort"*; `is_hidden()` printed two GLib CRITICALs per ordinary file; **the daemon dropped every plugin's reason for refusing, so SFTP and FTPS too said only "Denied"** (`VfsError::Denied(String)`); and a listing that failed to *re*-scan had nowhere to say so, so a share whose server had gone away showed as an empty folder (the final `Count` carries `error` now — true of a disk unplugged under a local folder as well). **Ports**: gvfsd-smb honours `smb://host:port/share` and the Host field is passed as typed, so `nas:4450` works; there is no Port field (owner's call whether there should be). SMB2 or later only (`25-smb.md`).
- **WebDAV**: removed from 0.1.0 (owner, 2026-09-21). No session to hold.
- **SFTP sign-in** (plan 29 L) against a real server: keys, password, keyboard-interactive, the trust step, Add versus Add and Connect.
- **LocalSend**: removed from 0.1.0 (owner, 2026-09-21). No phone to try.
- Mail through Thunderbird's composer; Tailscale to a second node.

### Phase 9 — the documents agree with the build (1 day)

- A **Status** line under every plan's title (table below).
- `CORE.md`: both open questions are answered — the PKGBUILD depends on `gnome-keyring` and `libsecret`; kiki's chooser sits *beside* GTK's, per user, with gtk as the fallback — so record them under Decisions and empty the section. Replace the stale plan-17 decision with decisions 1–6, and add: **kiki has no built-in code viewer and will not grow one** — a decision about what kiki is, recorded under Decisions rather than "Out of scope for 0.1.0", so nobody reads it as "later". Row 13 of the build order becomes "Editor bridge", and `CORE.md:80`'s mention of the viewer goes. "Out of scope for 0.1.0" gains: MTP, AFC and PTP; the Jarvis panel; Messages and AirDrop; spring-loaded folders; LocalSend receiving; Swap, "last mirrored" and the mirror options; the divider nudge; the pane-header branch chip; `ssh-agent` and `~/.ssh/config`; the `smb://` handler; AFP; and whichever of D1–D23 stand. Fix the architecture box (kinds), the `vsftpd` prerequisite, and rows 17, 18 (garbled), 19, 24, 29.
- `README.md`: Status rewritten from the suites' real numbers; Jarvis out of line 5; the kinds line; an install section, the keymap, and the location and mirror workflows (plan 10's "Docs").
- `API-DAEMON.md`: add `AiOpen`, `OpenTerminal`, `SetLocationImage`, `JobEvents`, `ClearJobs`, `DismissJob`, `JobLog`, `LocationLog` and the new job fields; remove `AiQuery`, `AiCancel`, `IndexProgress`, `SharePluginsChanged`, the `Read`/`Write` streaming text; `compress.format` gains `tar.bz2`. `API-PLUGIN.md`: the `Log` notification and what a plugin must never put in it; `Field.page`, `kind: "location"`, the share `Describe` fields (`requires`, `unavailable` — `defaultEnabled` went with LocalSend, 2026-09-21), `smb` as shipped (`dav` is not), and the truth about `LOCATION_KINDS` — a dropped-in location plugin is ignored by a release build.
- Plan amendments: 01 (paths, the `Backend` trait as it is, Prefetch struck); 02 (Side by Side, IPC target `shell`, the share and AI rows); 03, 04 (activity → plan 32); 05; 06 (kinds, agent auth and `splice` removed); 07, 23, 24 (the rename; 24's reversed Verification line; D18, D19 and plan 23's five other doc/code disagreements); 08 (entry point, Edit rules…); 10 (dependency lists); 12 (D5); 13 (retitled "Editor bridge": the viewer, the grammars, `OpenText`/`TextFind` and the Code tab's Verification bullets are deleted, with one line saying why; what is left is the nvim session, `F4`, and its gaps — `follow` stored and never read, no fallback to the `system` entry when `nvim` is absent, the socket not removed on close nor made 0600 — of which the socket is fixed in phase 2 and the rest are post-0.1.0); 03 and 23 lose their Code-tab lines, and 03 its "editable name in the header" (D7); 14, 16, 20 (what is post-0.1.0); 15 (toast); 18 (UI section); 21 (gallery, off while side by side); 22 (the `t^1.5` formula, the Accessed field); 27; 29 K (Favorites, undo, spring-loading), 29's drag table and `tst_DragDrop` list (link is out of 0.1.0 — phase 5's lines 222, 223 and 252) and 29 H (what exists).
- **Added 2026-09-21, from phases 5–7 and the owner's decisions that day**: plan 29 K's drag table (the `Alt` and right-button rows and the pointer hint out; Shift cannot force a move between machines); plan 07 (undo: a copy to a server yes, remote moves no; the pane-header badge line); plan 04 (the `deleteCopies` inverse); plan 08 (five at once and no selector, the offset row, the rules dialog and its defaults, `Copy/New`, `MirrorFilters`); plan 20 (no `Keymap` request; the settings pages that do not exist); plans 02 and 23 (no shortcuts overlay — `^?` opens the rebinding window); plan 24 (D18: Gallery); plan 10's installed-files table, plan 30's W1, `CORE.md` rows 18, 25 and 30, `docs/SKILL.md` and the Share mockup under `docs/design/` (all still name LocalSend or WebDAV); `API-DAEMON.md` (`OpenIn`/`OpenInTest`/`OpenInClose` take `tool`; `Keymap` gone; `MirrorFilters`/`SetMirrorFilters`; the `deleteCopies` op); `11-testing.md` (the suites as they are, and why `pointer_ops` skips). Plans 14 and 15 were amended on the day.
- Plan 19 becomes a stub: *not in 0.1.0 — replaced by "Open AI here…" (plan 29 L2)*.

### Phase 10 — package, install, check, tag (1 day)

- `cd packaging && makepkg -f` on a clean checkout; install the package; everything below is run against the **installed** kiki.
- Launch checks from phase 2: a second `kiki` raises the first window; `xdg-open ~/` opens kiki and a file keeps its own handler; nothing was set for root.
- Plan 29 D's checklist, plus: a physical drag between the panes, local → remote, of a few thousand files with the orb up, cancelled halfway, both ends checked; drag out to a terminal and in from a browser; **phase 5's drag feel, by hand on Hyprland** — (1) the cursor shows the theme's copy and move cursors as the target and the keys change, and "not allowed" over the folder a file is already in; (2) a right-button drag behaves as a left one; (3) Escape mid-drag leaves nothing moved, and no folder outline left lit; (4) a drop into the pane without the focus gives it the focus; (5) a plain drag onto another disk or server copies, Ctrl copies, Shift within a disk moves — the key table measured under `minimal`, confirmed under QtWayland; every theme eyeballed once (it stands in for D20's visual layer); `omarchy-theme-set` twice with kiki open; project mode once (D2).
- A session's shell log free of plan 29 F's warnings (or the portal one recorded as not ours).
- Release notes: what ships; what is hand-verified only (gio; FTPS mirroring if pyftpdlib was absent; the physical drag); what is not in 0.1.0. Tag.

**After the tag, 0.1.1**: 30 W6–W8 on a quiet tree, one commit each, before/after screenshots identical.

## Size

| Phase | Days |
|---|---|
| 0 Clean tree | ½ |
| 1 Build matches decisions | 1½ |
| 2 Defects | 5 |
| 3 Jobs, the job log and the orb | 4½–5 |
| 4 Large transfers | 3½–4 |
| 4a A changing folder; scrolling a long one | 1 |
| 4b Decorations; the thumbnailer's own process | 1 |
| 5 Drag and drop | 2½ |
| 6 Side by Side and mirror driven | 3½ |
| 7 Test debt | 3½ |
| 8 Real servers and devices | 1–2 |
| 9 Documents | 1 |
| 10 Package and tag | 1 |
| **Total** | **30–32** |

If that is too long, the cut line, in the order it costs least: phase 7 down to watch, undo and git badges (−2); `transfer_download` and `transfer_upload` folded into one remote flow (−½); D8 to a doc amendment (−½); the sway spike dropped and the skip accepted (−½). Phases 1–3, the move-semantics fix in 4, and phases 5 and 10 are not cuttable: they are the decisions taken, or defects that lose data.

## Status lines (written into each plan in phase 9; "unverified" must be gone, or explained, by the tag)

| Plan | Status at the tag |
|---|---|
| 01 Daemon and listing | built and tested; perf budgets measured in `bench`, not asserted; Prefetch struck |
| 02 Shell and views | built and tested; launch-to-paint timing unmeasured |
| 03 Inspector | built and tested; Created field not in 0.1.0; the name is read-only by decision (rename lives in the views) |
| 04 Operations and undo | built and tested; parallel small-file copy not in 0.1.0; activity → plan 32 |
| 05 Archives | built and tested |
| 06 Remote locations | built and tested against real `sshd` and FTPS; agent auth not in 0.1.0 |
| 07 Split mode | built and tested as Side by Side (plan 29 J) |
| 08 Mirror | built and tested, local and SFTP; Edit rules… not in 0.1.0 |
| 09 Omarchy integration | built; portal and FileManager1 hand-verified on the installed package; D-Bus activation not in 0.1.0 |
| 10 Polish and packaging | built; installed from the package once |
| 11 Testing | daemon, QML and e2e layers built; visual, layout and shell-performance layers not in 0.1.0; pointer e2e per phase 5's answer |
| 12 Search | built and tested as trimmed |
| 13 Editor bridge | no built-in viewer, by decision; nvim bridge built, hand-verified |
| 14 Open in | core built and tested; Settings page, watcher, `tab`, `follow` not in 0.1.0 |
| 15 Git status | built and tested |
| 16 Project mode | built, hand-verified; the rest of the plan not in 0.1.0 |
| 17 Devices | not in 0.1.0 (plugins stay in the tree; detection off) |
| 18 Share | built and tested; each way sent once for real |
| 19 AI query | not in 0.1.0 |
| 20 Settings | 8 pages built and tested; Locations, Open in, Plugins not in 0.1.0 |
| 21 View memory | built and tested |
| 22 Access heat map | built and tested; Accessed field not in 0.1.0 |
| 23 UI refinement | built and tested |
| 24 Mirror view | built as Side by Side; mirror bar, Swap, "last mirrored" dropped |
| 25 SMB | built, hand-verified against one SMB server (SMB2 or later; WebDAV is not in 0.1.0); no automated test |
| 26 Benchmarks | daemon half built and in CI, with a transfer profile; shell half not in 0.1.0 |
| 27 Gallery | built and tested; zoom, video poster, remote large preview not in 0.1.0 |
| 28 UI test coverage | built; drags by IPC hook and by hand |
| 29, 30 | closed by this plan; 30 W6–W8 in 0.1.1 |
| 32 Activity | built and tested |

## Verification

- `make test` is green, offscreen, with clippy at zero warnings inside it; the e2e default run includes `side_by_side`, `mirror_local`, `remote_transfers`, `drag_between_panes` and, where `sshd` exists, `mirror_sftp`; `transfer_local`, `transfer_download` and `transfer_upload` pass by name on the large fixture with throughput and peak RSS recorded beside plan 26's baselines.
- `plugin::LOCATION_KINDS` is `ftps`, `sftp`, `smb`; each has been opened against a real server once; no Devices section shows; every kind not shipped — WebDAV and AFP among them — is under "Out of scope" in `CORE.md`.
- No `kiki-plugin-highlight`, `OpenText` or `TextFind` anywhere in the tree or the package, and no plan promises a code viewer.
- No `AiPanel`, `AiQuery` or "Jarvis panel" anywhere in the tree; "Open AI here…" opens a terminal with the chosen tool.
- The orb shows idle, running and failed correctly through a transfer that is cancelled and one that hits a read-only file; the popup's entry shows the current file, its bytes and a rate, ends on the right completion line, and Reveal selects what arrived. Its Log shows kiki's own lines and the SSH or FTP library's beneath them, for that job and no other, with no password anywhere in it; a job that failed overnight can still be read from `failed-jobs.log`.
- Dragging between the panes: the default and each modifier match plan 29 K's tables for every local/remote pair, **less the link row**, shown while dragging; the receiver takes the focus; remote files on the trash ask first. Nothing springs open, and nothing offers to link.
- A remote move verifies before it deletes; a cancelled upload leaves the browser listing; undoing an extraction into an existing folder removes only what it added.
- `git commit` in a terminal clears the badges and updates the chip without a re-list; the column view shows the same marks as the list.
- Installed from the package: a second launch raises the first window; root's MIME defaults are untouched; plan 29 D is ticked line by line.
- Every plan in `docs/0.1.0/` carries a Status line matching the table above, `activity-view-spec.md` is gone, and `README.md`, `CORE.md` and both API documents describe the build that was tagged.
